# 交接：A 档图表类型已全部落地、DSL 已同步（2026-09-25）

> 给**新会话**的入口。这一轮把 `plans/chart-type-candidates.md` 里 A 档的数据型图全部做完并发了版，
> 随后把 `calendar` / `alluvial` 也同步进了 **DSL**（0.6.5，见 §4 —— 那节现在只是**闭环记录**，
> 没有待办的活）。读完这一页再动手，别重新调研；**下一件该做的事在 §5 末尾**。

## 1. 当前状态（两个仓库都是干净的 main）

| 包 | 版本 | 内容 |
|---|---|---|
| `@damoqiongqiu/ice-chart` | **0.30.16** | 分布组图（violin/beeswarm）、面板矩阵（matrix + 小倍数）、六边形分箱（hexbin）、边际分布（组合写法）、日历热力（calendar）、多轴分类流（alluvial） |
| `@damoqiongqiu/ice-chart-dsl` | **0.6.5** | 跟版到 `^0.30.16`：violin / beeswarm / hexbin / **calendar / alluvial** 的 kind 与编译；matrix 分面 |
| 技能卡 `ice-chart-dsl` | **0.2.4** | https://skills-hub.ai/skills/ice-chart-dsl-4（同一条目在涨版本，`info ice-chart-dsl` 可查全部版本） |

两个仓库远端都是 `origin`(Gitee) + `github-origin`(GitHub)，`main` 已同步；tag 打到 `v0.30.16` / `v0.6.5`。

## 2. 本轮落地清单（谁在哪份文档里）

| 能力 | 版本 | 文档 | 示例页 | 关键纪律/坑 |
|---|---|---|---|---|
| `violin` / `beeswarm` | 0.30.13 | `docs/06-chart-types.md` §2.1；根 `AGENTS.md`「分布组图」 | `examples/distribution.html` | 密度在数据空间、避让在像素空间；轮廓进域取**网格端点**；蜂群宽度封顶 + 确定性退化 |
| `option.matrix`（小倍数） | 0.30.13 | `docs/06` §8；`AGENTS.md`「面板矩阵」；spec `docs/superpowers/specs/2026-09-24-small-multiples-matrix-design.md`；plan `docs/superpowers/plans/2026-09-24-small-multiples-matrix.md` | `examples/matrix.html` | 每面板一套 scale（域共享）；轴只画外圈；`matrix` 缺席时逐像素不变 |
| `hexbin` | 0.30.14 | `docs/06` §2.2 | `examples/hexbin.html` | **像素空间分格**（缩放重分格）；命中 O(1)；交互单位是**格子** |
| 边际分布（joint plot） | 0.30.15 | `docs/06` §8.1 | `examples/joint-plot.html` | 组合写法（matrix 权重 + 第二个 y 轴 + 共享 x）；**每行画自己那根 y 轴**；`bar` 会让 x 轴推断成类目轴 → 必须显式 `type:'value'` |
| `calendar` | 0.30.16 | `docs/06` §2.3 | `examples/calendar.html` | 日期按 **UTC 的 Y/M/D**；命中是反解（与格数无关） |
| `alluvial` | 0.30.16 | `docs/06` §2.3 | `examples/alluvial.html` | 表的分面（对比桑基=图的拓扑）；轴排序用**重心法**；命中节点优先、带子用两侧区间+插值 |

## 3. 验证口径（照抄就行）

```bash
# core：lint → types → build → jest
npm run verify                      # 当时结果：73 套 / 644 条
# 全量交互审计（36 个示例页）
npm run build && npm run examples:prepare && node scripts/serve-examples.cjs &
node scripts/audit-interactions.mjs ./.audit-full   # 当时结果：388 步 / 0 问题
```

**发版节奏**（AGENTS 有正式版本，这里是实测节奏）：
开发分支 → `npm run verify` → 合并 main → CHANGELOG 定格 + 版本号 + `chore(release)` 提交 →
tag → 推 `github-origin` 与 `origin` → `npm publish --access public` → **以 packument 为准**
（`curl registry.npmjs.org/@damoqiongqiu%2Fice-chart`，看 `dist-tags.latest`）。
⚠️ 今天几次发布都出现了「PUT 202 → packument/tarball 5~10 分钟才可见」的窗口：**别急着重发**。

## 4. DSL 同步：已闭环（0.6.5，2026-09-25）

`calendar` / `alluvial` 已经进 `ice-chart-dsl` 的编译清单，**这一节没有待办**；留着是为了让下一个人
知道「当时怎么定的」与「哪条路走不通」，不必重新推一遍。

### 定下来的 encoding 形态（该仓 README 的 kind 表已同步）

**列名一律写在 `encoding` 里，观感配置走各自顶层的同名字段**：

| kind | 通道 | 顶层观感字段 |
|---|---|---|
| `calendar` | `encoding.x` = 日期列、`encoding.y` = 数值列（一行一天） | `calendar: { weekStart?, weekdayLabels?, monthLabels?, minColor?, maxColor? }` |
| `alluvial` | `encoding.axes` = 轴列名数组（≥2，按顺序）、`encoding.value` = 流量列（可选） | `alluvial: { sort?, spread?, nodeWidth?, gap?, ribbonOpacity? }` |

**为什么不照抄 core 的字段名**：core 的 `alluvial.rows` 是**记录对象数组**（`row[轴列名]`），
而用户手上是 `{ columns, rows }` 的两维表 —— 把「表 → 记录对象」这个转换放在编译期，
DSL 才真在做数据绑定；写成 `options.alluvial.axes` + 表整块透传就等于把绑定责任推回给调用方，
撞该仓铁律 1（不做 ChartOption 的马甲）。日历同理：**日期不在 DSL 里解析**，
core 的归一化是唯一事实来源（UTC 的 Y/M/D、同日取和），所以 `Date` 与 `2026-1-5` 都还能用。

### 诊断（口径：报 core 会**静默跳过**的那批数据）

- 错误：`alluvial-axes-too-few`（轴 < 2）、`invalid-axes`（`axes` 写成单个列名）、
  `unknown-column`（轴列不存在，带可用列名）、日历缺数值列；
- 警告：`calendar-unparseable-date`（带行数 + 建议 `YYYY-MM-DD`）、
  `alluvial-single-category`（单类目轴 = 一条直线）、`alluvial-missing-axis-value`
  （正是 core 会跳过的那批行）。
  日期判据**故意比 core 宽松**（只做它的兜底那一步 `Date.parse`），方向单向：DSL 说「不像」的，
  core 一定也读不出来 —— 宁可漏报不可误报。

### 收尾时踩到的两件事

1. **`skills-hub version` 读的是 SKILL.md 里的版本号**：不先改文件就跑，会报
   `Version 0.2.3 already exists`。顺序是「先改 front matter → `version` → `publish`」。
2. `publish` 打印的 `ice-chart-dsl-4` 是**这次发布的记录页**，不是新条目 ——
   `info ice-chart-dsl` 里看得到 v0.2.2 → 0.2.3 → 0.2.4 在同一条目下涨。

### 验证与发版记录（照这个口径复核过）

- 该仓 `npm run verify`（types → build → jest）**70 条**全绿、`npm run test:e2e` 2 条通过；
  另跑了真机浏览器（临时页，已删）：日历 120 格 / alluvial 21 个点、无 console 报错、两张图都真落墨。
- 合并 `6bc96e3` → 发版 `5377bdb`（CHANGELOG 定格 0.6.5 + 版本号）→ tag `v0.6.5` → 双远端 →
  `npm publish` → **packument 核验**：约 **3.5 分钟**后 `dist-tags.latest` 才指过去（当天几次发布都是
  这个量级，别急着重发）；随后下载 tarball 确认 `peer ^0.30.16` 与新 kind / 诊断都在产物里。

## 5. 未做 / 已知边界（别当成 bug）

- **右侧边际**（joint plot 的右半）：需要「面板级柱形方向」（横向柱），要动 bar 的方向假设 —— 记在 `docs/06` §8.1。
- **矩阵 + 多 y 轴**：矩阵下每行画自己的那根轴，但同一行内多个面板用不同轴时以**左列**为准（轴只画一份）。
- **面板独立 y 域 / 面板级独立缩放 / 数据驱动 facet**：facet 归 DSL（已在 0.6.4 用 `matrix` 实现）。
- 设计期的临时账本在 `.superpowers/sdd/`（git-ignored，可删）——里面是当时逐任务的 rulings 记录。
- 本轮修掉的坑（都已在文档里）：提示框避让边界、x 轴抽稀按并集、单面板轴被藏掉、
  `mean` 聚合的 O(格数×点数)、审计截图误提交（`.gitignore` 已补 `.audit-*`）。

### 下一步候选（2026-09-25 收尾时盘出来的，都没开工）

1. **本仓 `README.md` 的「路线图」已经陈旧**：日历热力已经作为 `calendar` 落地，却还挂在
   「标注的第二梯队」里；「把列存推广到折线 / 面积」也早已实现（`LineSeries` 有完整的 virtual 分支，
   10 万点折线实测 16.7ms/帧）。路线图那一节还停在 0.30 之前的能力清单，值得按
   `AGENTS.md` 的「已实现 / 未实现」重写一遍。
2. **`ice-chart-dsl` 的示例页没跟上**：`examples/chart-dsl.html` 只演示到 `sankey`，
   最近四轮新类型（violin / beeswarm / hexbin / calendar / alluvial）一个都没进页面，
   真机冒烟（`e2e/examples-smoke.spec.ts` 按目录驱动）因此覆盖不到它们。
3. **本仓性能路线 P1~P3**（`plans/incremental-pipeline.md`）：布局复用、组件同步的差异更新、
   归一化的结构性跳过。10 万根窗口的验收线（≤ 0.5 ms/tick）仍未达到。
4. 桑基节点拖拽重排与折叠、`appendData` 的增量绘制、y 方向 dataZoom 的 UI —— 见 `AGENTS.md`
   的「仍未实现」（隐函数 / 等值线属于 B 档，要做就另开包，不进核心库）。
