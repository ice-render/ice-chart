# Changelog

## [Unreleased]

> 下一个版本发布前，改动在这里累积。

### 新特性

- **y 方向的 dataZoom 滑块**（`dataZoom.sliderY`）：绘图区**右侧外**一条竖直轨道，
  拖动改 y 的数据窗口（拖手柄改跨度、拖窗口平移、点轨道居中），与底部那条横向滑块同一套语义。
  默认**不显示**（多让出一条竖直轨道会改绘图区宽度，不该让既有图的外观变一次），
  `sliderY: {}` 打开、`show: false` 显式关；`width` 默认 26、`axisIndex` 在多 y 轴时选驱动哪一根。
  方向约定「上 = 大值、下 = 小值」（与屏幕同向），所以从上往下拖 = 窗口朝小值走 ——
  与横向那条的符号正好相反。

  **类目 y 轴不出竖直滑块**：归一化有一条既有规则「类目 y 轴不吃 y 视窗」
  （`applyViewToNormalized` 的 `axis.categoryDomain` 短路，带视窗重跑那条路一致），
  给它一条能拖但拖了没反应的轨道只会骗人，所以布局直接不预留 ——
  「窗口一个横向柱排行榜」要动的是那条归一化规则，仍未做。

### 修复

- **双峰小提琴悬停不上（锚点落在自己的形状之外）**：小提琴的像素缓存（高亮与提示框的锚点）
  一直取中位数的 y，而双峰分布的中位数正落在两个峰之间的**谷底** —— 那里核密度≈0、轮廓宽度收成 0，
  锚点就落到了自己的多边形之外：形状看得见却悬停不上，提示框与高亮也跟着飘在形状外。
  现在锚点取**峰值密度那一行**（轮廓最宽处），与 `rebuildPixels` 注释里一直写着的意图一致。
  单峰分布里中位数本来就靠近峰，所以这个坑只在双峰上暴露（`distribution` 页的
  「双峰（带宽 ×0.3）」实测 `hitTestIndex` 返回 -1、同一组最宽处返回 1）。

- **`dataZoom.slider.color` 以前是「文档里有、代码里没用过」**（组件根本没有颜色字段）——
  现在横竖两条滑块的选中窗口都吃这个颜色（不配仍走主题色板）。

## [0.30.16] - 2026-09-25

> 接 0.30.15：A 档候选里最后两块数据型图 —— **日历热力**（`calendar`）与
> **多轴分类流**（`alluvial`）。两者都是自带坐标语义的场景（不画直角坐标轴）。

### 新特性

- **日历热力 `calendar`**：一行一天（周为列、星期为行）。数据是 `{ date, value }` 明细表；
  日期按 **UTC 的 Y/M/D** 解释（跨时区结论一致）；命中是反解（两道除法 + 查表），
  「多年 × 每天一条」也是常数时间；同一天多条取和，坏日期跳过不抛异常。
  可配 `weekStart` / `weekdayLabels` / `monthLabels` / `minColor` / `maxColor`。
- **多轴分类流 `alluvial`**：`alluvial: { axes, rows }` —— **表的分面**（对比桑基是图的拓扑）。
  节点按流量堆叠、跨轴流量按 (from, to) 聚合；轴排序用**重心法**（一趟，不做会交叉成一团）；
  命中节点优先、其次带子（两侧区间 + 横向插值判定）；带子按源节点着色。
  示例页 `examples/calendar.html` / `examples/alluvial.html`。

## [0.30.15] - 2026-09-25

> 接 0.30.14：**边际分布（joint plot）**不需要新类型 —— 它是面板矩阵 + 第二个 y 轴的组合写法；
> 但拼的过程中抓到一条真 bug（矩阵里每行恒画主轴），这一版修掉，并把拼法与两个坑写进文档。

### 修复

- **面板矩阵：每行画它自己那根 y 轴**。以前每行恒画主轴，于是边际分布（joint plot）里
  边际那一行会顶着主图的量程 —— 计数 70 被读成「规格 B ≈ 70」，数字全对不上。
  现在按「该行左列面板里的系列用哪根 `yAxisIndex`」来决定（那一行没有系列就不画轴）。

### 文档 / 示例

- 新增示例页 `examples/joint-plot.html`：**边际分布（joint plot）是组合写法，不需要新类型** ——
  `matrix: { rows: [3, 1] }` + 第二个 y 轴（计数域）+ 共享 x 域；直方图分箱在页面侧算。
  拼法与两个注意点（每行各画自己那根轴、`bar` 会让 x 轴推断成类目轴所以要显式 `type: 'value'`）
  写进 `docs/06-chart-types.md` §8.1。

## [0.30.14] - 2026-09-25

> 接 0.30.13：补上 A 档候选里的**六边形分箱**（`hexbin`）—— 点云聚成蜂窝格，
> 分箱在像素空间做、命中 O(1)，交互单位是格子。

### 新特性

- **六边形分箱 `hexbin`**：点云聚到蜂窝格上（格的深浅 = 计数或聚合值），数据形态与散点一致
  （`[[x, y]]` 或 `[[x, y, 权重]]`）。
  - 分箱在**像素空间**做（`src/layout/hexbin.ts` 纯函数）—— 按数据空间分格会被比例尺拉成菱形；
    代价是缩放会重新分格，这正是密度视图该有的行为；
  - **命中 O(1)**：像素 → 轴向坐标是闭式解 + 立方体取整；交互单位是**格子**（`pixels` 存格心），
    提示框给该格的聚合值与点数；
  - `hexbin: { radius（默认 14）, aggregate: 'count' | 'sum' | 'mean' | 'max', minColor, maxColor }`；
    默认色阶与热力图共用（`heatColorRange`）；示例页 `examples/hexbin.html`。

## [0.30.13] - 2026-09-24

> 两块新能力：**分布组图**（`violin` / `beeswarm`，可与箱线叠成雨云图）与
> **面板矩阵**（`option.matrix` + `series[].panel`，小倍数 / 主图 + 窄条）。
> 两条都守「不给新配置就逐像素不变」的回归基线；文档回写在 `docs/06-chart-types.md`
> §2.1 与 §8，纪律回写在根 `AGENTS.md`。

### 新特性

- **分布组图：小提琴图 `violin` + 蜂群图 `beeswarm`**（可与已有的 `boxplot` 叠成雨云图）。
  - `violin`：数据项是**一组原始观测值**（`number[]`，也收 `{ name, values }`），
    轮廓来自高斯核密度估计 —— 带宽默认走 Silverman 经验法则
    `0.9 · min(σ, IQR/1.34) · n^(-1/5)`，可用 `violin.bandwidth` / `violin.samples` /
    `violin.side`（`both` / `left` / `right`）调；提示框给**观测数 + 五数概括 + 峰值密度**。
  - `beeswarm`：逐点 `[x, y]`（与散点同形），同一类目里挤在一起的点横向避让一个直径；
    宽度由 `spread`（占一个类目的比例，默认 0.8）封顶，超容量时退化成**确定性**抖动
    （同一份数据每次画出来一样）；点比像素列多时按散点那条密度规则落墨，命中与提示框仍走全量。
  - 两者都是**直角坐标场景**，与柱形 / 箱线共用类目轴；命中判定与渲染共用同一份几何
    （小提琴按轮廓多边形内部，蜂群按像素最近邻）。密度与避让是纯函数
    （`src/layout/density.ts`：`computeKdeProfile` / `computeBeeswarmOffsets` / `silvermanBandwidth`），
    可单测。示例页 `examples/distribution.html`。
- **面板矩阵（小倍数）：`option.matrix` + `series[].panel`**。一张画布放 N 个同构面板，
  **共享同一份数据域**（刻度一致、可横向比较）：
  - `matrix: { rows, columns, gap }`：数字 = 等分，数组 = 权重 —— `columns: [4, 1]` 就是
    「主图 + 右侧窄条」，也是边际分布的几何底座；
  - 轴只画外圈（每行一份 y 轴、每列一份 x 轴），网格每个面板一份；x 轴标签按**一列的宽度**抽稀，
    窄于 128px 的列不画 x 轴（它和主图共享同一条 x 轴）；
  - 交互按**指针所在面板**解析：轴触发提示框只列那块的系列、准星只画在那块里、
    缩放 / 平移的锚点用那块的比例尺；框选 x 夹到所有面板的并集（刷一段 x，六块一起看）；
  - `addMark` 支持 `panel`；不给 `matrix` 时**逐像素不变**（回归基线）。
  - 示例页 `examples/matrix.html`；v1 不支持面板独立 y 域 / 面板级独立缩放 / 数据驱动 `facet`（归 DSL）。

## [0.30.12] - 2026-09-24

> 接 0.30.11：平移 / 缩放不再重建点集（10 万点 **3.4 → 1.0ms/tick**）；
> 并把「帧 ≠ 流水线」这笔账与三种取点策略的实测写进施工图（别再改取点策略，见 `plans/`）。

### 性能

- **文档**：`docs/` 从「两篇专题」扩成一套**技术与架构文档**（索引在 `docs/README.md`）：
  架构与分层 / 更新流水线 / 渲染与组件契约 / 交互与命中 / 数据形态与大数据 / 图表类型与场景坐标 /
  主题与无障碍 / 测试与发版，外加既有的标注与列存两篇专题。每篇都有**目录**、小节一律**编号**；
  README 的「架构」一节与根 `AGENTS.md` 都加了入口指路。

- **只换视窗时不再重建点集**：`buildPoints` 每趟归一化都要给每个普通系列物化一遍
  `DataPoint`（10 万点约 1.5~2ms 加一堆垃圾），而**平移 / 缩放 / 重复归一化时数据根本没变**。
  现在按「**数据数组身份 + 长度 + 解析规则**（`type|xField|yField`）」复用上一趟那批点 ——
  10 万点只换视窗：**3.4 → 1.0 ms/tick**。滑动窗口不受影响（每 tick 换新数组，本来就不该复用）。
  口径与类目域那套同源：换了数组 / 换了长度 / 换了字段一律重建；
  `radar / sankey / graph` 的点由旁边的配置块决定，不在复用范围内。

## [0.30.11] - 2026-09-24

> 接 0.30.10：把「数据一变就重跑整条流水线」这条地板再压一层 ——
> 一次更新只跑一遍归一化、类目域改成增量维护、并新增 `chart.batch()` 同帧批合并。
> 10 万点普通路径：单来源真滑窗 **10.5 → 4.2ms/tick**，多系列各自 `setData` **45.4 → 10.4ms/tick**。

### 新增

- **`chart.batch(fn)`：同帧批合并**。一帧里对多个系列的更新（`setData` / `appendData`）
  现在可以合成**一次**流水线。

  ```ts
  chart.batch(() => {
    chart.setData('k', candles);
    chart.setData('volume', volumes);
    chart.setData('ma7', maValues);
  });
  ```

  为什么需要（实测，10 万点 × 3 系列、滑动窗口）：逐系列调用是 **45.4ms/tick** ——
  每次调用都跑一整条「归一化 + 布局 + 同步」，而且**中间态里各系列天然不对齐**
  （第一条已经滑过去、第二三条还没），类目域的「逐项同一」增量因此也用不上。
  包进 `batch()` 之后：**45.4 → 10.4 ms/tick**（−77%），并且流水线看到的是全部更新完的一致状态。

  语义：`batch()` 返回时图表已经是新状态（**同步**）；批内不发布 `data:change`，
  出批后按调用顺序补发（监听者读到的图永远自洽）；批内抛异常也会 flush（`finally`）。

### 性能

- **单来源普通系列的类目表改成增量维护**：真滑窗（头部淘汰 + 尾部追加）以前每 tick 都要把
  10 万类目重新去重一遍，还要顺带建一张 10 万条的 `Map`（函数级 profile 里 `buildXDomain`
  自身占整帧 **38.7%**、实测 2.9ms/tick）。现在照惰性原始点那套成熟口径（`values` +
  **绝对序号** `seq`，下标 = 序号 − 首项序号）增量维护，「删头 / 补尾」各 O(delta)，
  而且**逐项验过**才认 —— 对不上 / 出现重复类目 / 换了数据源就退回全量聚合。

  | 口径（普通系列，每 tick p50） | 改前 | 改后 |
  |---|---|---|
  | 10 万点真滑窗 | 7.8 ms | **4.2 ms** |
  | 1 万点真滑窗 | 1.1 ms | **0.9 ms** |
  | 头部不动、只换尾巴（验证不过 → 退回全量） | 7.55 ms | 7.07 ms（不退化） |
  | 惰性原始点 + 环形（±视窗） | 0.5 / 0.3 ms | 0.5 / 0.3 ms（不变） |

  （配合上一版「一次更新只跑一遍归一化」，10 万点真滑窗从 **10.5 → 4.2 ms/tick**。）

- **多来源但「x 逐项相同」时，合并域也走增量**：K 线 + 量柱 + 均线共用同一批 x 的形状，
  以前每帧都要把每张表再和第一张合并一遍（逐点 `String()` + 哈希）。现在先算第一条的增量表，
  再**逐项验证**其余来源与它同一 —— 同一就直接用那张表，不必再合并。
  10 万点 3 系列、同帧批更新（`setOption` 一次带全部系列）：**16.5 → 11.4 ms/tick**（同一版本
  里「强制全量合并」的对照）。多个系列**各自** `setData` 的写法中间态天然不对齐，
  那条路请用既有的 `series[i].xFrom` 声明（实测 3 系列 10 万点 51.8 → 28.5 ms/tick）
  或改成同帧批更新。

### 修复

- **类目（合并）域的缓存键不健全**：普通系列只按「点数 + 首末 x」记键，
  于是「长度、首、末都没变、中间变了」（批量替换一段、按窗口重取）会命中**过期的域** ——
  类目轴上少/多一个类目，而且再也自愈不了。现在普通系列按**数据数组身份**记键，
  轴自己声明的 `axis.data` 也把身份记进去。门禁：`tests/option/category-domain-cache.test.ts`
  （单来源 / 多系列 / `axis.data` 三条；另外两条滑动窗口的等价性护栏）。

- **一次数据更新只跑一遍归一化**：`applyOption` 原来先把整条归一化跑一遍拿全域，
  `rebuild` 再跑一遍拿视窗域 —— 而第二遍的全部作用只是「把视窗套上去」，视窗在归一化里
  只落在三处：x 域裁剪（`resolveXDomain`）、y 轴显式域、表达式系列的采样区间。
  现在新增纯函数 `applyViewToNormalized`，把全域那遍的结果**套**上视窗即得，
  第二遍整条归一化不再跑（表达式系列与等比坐标由 `canApplyView` 挡下来，照旧整条重跑）。

  | 口径（普通系列） | 改前 | 改后 |
  |---|---|---|
  | `scripts/measure-pipeline.mjs` 10 万点 | 10.5 ms/tick | **7.8 ms/tick** |
  | `scripts/measure-pipeline.mjs` 1 万点 | 2.2 ms/tick | **1.1 ms/tick** |
  | 单测拆解 `setData`（10 万点） | 6.2 ms | **3.6 ms** |
  | 惰性原始点 + 环形（±视窗） | 0.6 / 0.3 ms | 0.6 / 0.3 ms（不变） |

  门禁：`tests/option/apply-view.test.ts`（与「带视窗重跑一遍」逐项等价，含横向图 /
  热力图这类类目 y 轴不吃 y 视窗的规矩）、`tests/chart/single-normalize.test.ts`
  （`setData` / `setOption` / `appendData` / `setDomain` 各只一遍）。

### 测量（顺手证伪的一条）

- **滑动窗口的数组拷贝不是成本**：10 万点上 `concat` + `slice(-n)` 只有 **0.055 ms**
  （原地 `push` + `splice(0,1)` 0.005 ms）。曾按「少搬一趟」把 `appendData` 改成原地追加，
  实测**买不到性能**、却会改掉调用方自己那个数组 —— 已退回，契约钉在
  `tests/chart/append-array-contract.test.ts`。
- 普通路径滑动窗口里真正的那 **3.5 ms** 是**类目域重建**：同数组 `setData` 4.0 ms vs
  滑动窗口 7.5 ms；数值轴对照 3.5 vs 3.2（没有类目可重建就没有这笔）。这是下一条。

## [0.30.10] - 2026-09-24

> 接 0.30.9：1M 根 + 量柱 + 均线 **66.7ms → 50.1ms/帧**；同时把「剩下那半时间在哪」量清楚了。

### 修复（性能）

- **类目淘汰不再扫整张表**：`removeCategory` 原来用 `categories.findIndex(String(value) === key)`
  —— 100 万类目的滚动窗口每追加一根都要逐项 `String()` 比较（实测 70ms/3 秒）。
  现在优先用**序号推算**下标（`序号 − 首项序号`，无空洞时就是它；一次减法），
  推不出来（中间类目被淘汰留下的空洞）或对不上时，照旧线性找 + 就地压紧。
  语义完全不变 —— `tests/option/raw-category-index.test.ts` 那套「与参照实现逐项一致」的
  不变式照旧守着（含中途淘汰）。

### 测量（剩下那半时间在哪：**canvas 光栅**，2026-09-24）

用 CDP `Performance.getMetrics` 拆「渲染进程任务时间」（1M 根窗口，`&volume=1&ma=1`）：

| | Task 3 秒 | 脚本 | 样式 | 布局 | 其余（光栅 / 合成 / GC） |
|---|---|---|---|---|---|
| 持续推流 | 3.00s | 1.55s | 0.000s | 0.001s | **1.45s** |
| 暂停推流 | **0.05s** | 0.01s | 0.000s | 0.000s | 0.04s |

两条结论：① 成本**全部来自「数据更新触发的重绘」**（暂停就归零）；
② 其中**一半是 canvas 光栅**（样式/布局都是 0 —— 不是 DOM 的锅）。
到此为止，剩下的优化属于**引擎的重绘策略**（整幅重绘 vs 滚动 blit），不在本仓这几条线的射程内。

## [0.30.9] - 2026-09-24

> 接 0.30.8：1M 根 + 量柱 + 均线 **233ms → 66.7ms/帧**（本版 + ice-trading-chart 0.4.4）。

### 新增

- **`series[i].xFrom`：声明「这条系列的 x 与另一条系列逐项相同」**（写那条的 `id`）。
  给派生系列用（均线 / MACD 的 DIF·DEA / 成交量……它们的 x 本来就是从主系列抄来的）。

  引擎据此把这些系列**从类目合并里摘出去** —— 百万点规模下合并是每帧最大的一笔
  （1M × N 次 `String()` + 哈希，实测 38~90ms/次、每帧好几次）。声明只在
  「被引用的系列在、且两者**点数相同**」时生效：抄错了只会退回合并（慢一点），
  **不会给出一张错的轴**。实测 1M 根 + 量柱 + 均线：**233ms → 66.7ms/帧**、
  `appendData` 287ms → 7.7ms。

### 测试

- `tests/option/normalize.test.ts` 新增 3 条：声明生效时轴域 == 被引用系列那张表 /
  点数对不上时忽略声明退回合并 / 被引用的系列不存在时忽略。全量 **498 用例** + 37 示例冒烟绿。

## [0.30.8] - 2026-09-24

> 接 0.30.7：1M 根 + 量柱 + 均线 **750ms → 233ms/帧**（本版 + ice-trading-chart 0.4.3 的
> 「派生系列不再裁首尾 null」合起来）。

### 修复（性能）

- **类目（合并）域加一次性缓存**（按存储指纹命中）：一次 `applyOption` 要归一化两遍，
  同一帧里视窗推进还会再来一遍，而多来源类目轴的合并要对每张表跑一趟 `String()` + 哈希
  （1M × 2 ≈ 38ms/次）。指纹取「长度 / `categoryNext` / 计数表大小 / 首末类目」——
  `categoryNext` 只在新增类目时自增、计数表只在增删类目时变，覆盖了滚动窗口的两头改动；
  判不中就重算（宁可多算一次，不给错的轴）。

### 还剩什么

类目域的**增量并集**：现在「K 线的表 vs 派生系列的表」每帧仍要合并两趟（各 1M 行）。
滚动窗口的改动只发生在两头（淘汰老的 / 新来一根），按 delta 更新并集就能把它降到 O(1) ——
这是 `plans/incremental-pipeline.md` 里 P4 之后的下一条。

## [0.30.7] - 2026-09-24

> 这一版接着 0.30.6 把「远景密度」补齐，并**更正 0.30.6 的帧时间口径**（见那一版里的 ⚠️）。
> 1M 根 + 量柱 + 均线：**≈2.2s/帧 → 0.75s/帧**（K 线单系列仍是 16.7ms/帧，有余量）。

### 新增

- **类目轴上的虚拟（`virtual: true`）折线 / 面积 / 散点**：x 不是数值时不再报错，
  而是退回**惰性原始点** —— 一样不建 `DataPoint` 数组，组件走虚拟绘制（每像素列抽样）。

  这条让「均线挂在时间类目上」这种形状的百万点系列也能进列存，而不是被逼回普通系列
  （那会物化 100 万个对象、每帧跑一遍 LTTB、还要建逐点像素缓存）。
  仍然拒绝的组合只有一种：**数值 x + 类目轴**（自相矛盾，照旧显式报错）。

### 修复（性能）

- **虚拟折线不再物化逐点像素缓存**：`LineSeries.doRender` / `renderSequence` 原来先
  `rebuildPixels()` 再走虚拟分支 —— 惰性原始点（raw）会真的建 1M 长的像素数组
  （实测 958ms/3 秒），而虚拟分支根本不读它。现在虚拟系列一律按需算像素。
- **类目表的合并去重支持「分组 + 顺手建查表口」**：多来源里**逐项相同**的表只留一份
  （`!==` 精确比较，不做字符串转换），只有真正不同的表才进合并；合并用的
  `Map<key, 下标>` 同时作为 `categoryLookup` 交下去，`BandScale` 不必再自建一张同规模的表。
- **数值列的取点规则与普通系列对齐**：`{ x, y }` 形状的**对象行**以前在数值列里
  解析不出来（x 退成下标、y 是整行 → 直接判定失败），现在按 `xField` / `yField` 读，
  与 `readGenericPoint` 逐字一致。

### 还剩什么（实测过的下一步）

1M 规模下的**帧**现在由流水线主导，两个方向（都在 `plans/incremental-pipeline.md` 里）：
**P4 同帧批合并**（同一帧里对多个系列的 `appendData` 合成一次归一化 —— 现在是 2 系列 × 2 遍 = 4 遍）
与**类目域的增量并集**（只按「淘汰了几个 / 新来几个」更新合并域，而不是每帧重扫两张 1M 表）。

## [0.30.6] - 2026-09-24

### 修复（性能：远景下不再逐根画）

- **柱子（`bar`）有了稠密模式**：类目轴上每像素超过 2 根柱子时，改成
  **每个像素列画一根聚合矩形**（该列所有柱子 `[min(base,值), max(base,值)]` 的并集），
  列内最多抽 8 个候选；同时：
  - 稠密模式**不再物化「每根一个」的像素缓存**（1M 根时那是 5.6ms/帧），
    命中改按 x 比例反推下标 + 验邻域几根，`pixelAt` 走虚拟系列的按需分支；
  - **渐变只在看得见的时候建**（柱高 < 3 设备像素用纯色）——
    原来每根柱子都 `createLinearGradient` + 2 个色停，1M 根实测 180ms/帧；
  - `barRectAt` 的像素循环改走**标量访问器**（原来是每根 `pointAt` 合成一个 `DataPoint`）。

  ⚠️ **更正（2026-09-24 晚间）**：这一条发布时写的「柱子 + 均线 3516ms → 16.7ms/帧」是
  **测错了** —— 那次 rAF 探针撞上了停摆的推流循环。真实情况：**绘制那半确实归零**
  （函数级实测 `fill` 1688ms → 0、每根一个渐变 180ms → 0），但 1M 规模下的**帧**
  由流水线主导（每帧 4 次归一化 + 每次合并两张不同的类目表 ≈38ms），
  当时是 ≈2.2s/帧。0.30.7 之后是 **750ms/帧**（见那一版）。非稠密路径逐像素不变。

- **类目表的多来源合并加了精确快路径**：所有来源的类目表**逐项相同**（`!==` 比较，
  不做字符串转换）时直接复用第一张表 + 它的查表口 —— 滚动看盘里 K 线 / 量柱 / 均线
  本来就是同一批 x，而合并去重要对每张表跑一遍 `String()` + 哈希（100 万 × 2 ≈ 200ms/次）。
  任何一处不同（哪怕只是 `1` 与 `'1'` 这种字符串相等）就退回原合并路径，结论逐项一致。

- **虚拟系列的 `nearestIndexAtX` 不再先建像素缓存**：`SeriesBase.nearestIndexAtX` 原来无条件
  `rebuildPixels()`，虚拟系列用的是按需访问器 —— 1M 点时每次 mousemove 白建一遍。

### 测试

- `tests/components/series.test.ts` 新增三条：稠密模式的触发条件、**每像素列一根聚合矩形**
  （落墨量 ≤ 像素列数、列内尖峰不丢）、稠密模式不物化像素缓存且命中仍给出真实下标附近的那一根。

## [0.30.5] - 2026-09-24

### 修复（性能：有视窗的滚动看盘）

- **视窗裁剪之后类目轴仍然走增量查表**（`categoryLookup` + `categoryOffset`）。

  类目查表口（第 0 期那条）原来只在「域原样来自增量维护的类目表」时才交出去 ——
  因为查表口是按**整张表**编号的，域一旦被视窗裁成一段，下标就不再对齐。
  而**滚动看盘正是「域=整张表的一段」**：每 tick 都要为 10 万条类目重建一张索引 Map
  （函数级 profile 里 `indices` 一项 3 秒 214.7ms，7%），比不带视窗的同类页面还慢。

  现在查表口连同 `categoryOffset`（域起点在整张表里的下标）一起交下去，
  `BandScale.indexOf` 用「全表下标 − 偏移」换算窗口内下标（越界就是不认，语义与自建表一致）。
  同一口径顺带铺到三处原本的全表 `indexOf`：`resolveXDomain`（两趟 O(n)）、
  `domainFractions`、`clampAxisDomain` —— 有查表口就走 O(1)，没有的还是老路。

  实测（`scripts/measure-pipeline.mjs` 新增的「惰性原始点 + 环形 + **视窗**」档位，
  真机 Chromium，宿主是 ice-trading-chart 的流式页）：

  | 档位 | 第 3 期前 | 第 3 期后 |
  |---|---|---|
  | 1 万点 每 tick p50 | 0.5 ms（流水线 0.22） | **0.2 ms**（0.11） |
  | 10 万点 每 tick p50 | **1.6 ms**（流水线 1.48） | **0.3 ms**（0.34） |
  | 不带视窗（10 万点） | 0.6 ms | 0.6 ms（**没变**） |
  | 普通（concat + 重建，10 万点） | 21 ms | 21 ms（**没变**） |

  门禁：`tests/scale/scale.test.ts`（偏移换算 + 越界不认 + `clone()` 带上偏移）、
  `tests/chart/stream.test.ts`（视窗裁剪后查表口仍在、映射与自建表逐点一致）。
  全量：**489 个用例**（55 个文件）全绿；`npm run verify` 干净。

## [0.30.4] - 2026-09-23

### 修复

- **直角坐标的系列一律裁到绘图区**（含自定义系列）。

  `SeriesBase` 的注释早就写着「直角坐标系列必须开 `clipToBox`」，但字段默认是 `false`、
  只靠每个内置类型自己声明 —— **`registerSeriesType` 进来的自定义系列会静默漏掉**
  （`BarSeries` / `LineSeries` / `ScatterSeries` 这些自己声明了，用户注册的没人提醒）。
  现在由图表按布局类型统一兜住（`norm.kind === 'cartesian'` 时打开）；
  饼 / 雷达 / 桑基 / 关系图 / 矩形树图这些没有「绘图区矩形」语义的场景一律不动
  ——它们的盒子虽然也是 plot，但要画到圆外、节点外，裁了就是画不出来。

  ⚠️ **诚实标注**：这条**不是**某个已观测缺陷的修复。当时是为了查 `editable-chart` 轴带里
  那 112px 才做的，做完发现**墨迹纹丝不动**（那个东西最后查明是交互手柄，不是系列）——
  它独立成立的价值只是**兑现基类注释里那条「必须开」的约定**，把自定义系列这个坑堵上。
  行为变化：打开后自定义系列的墨迹不再越出绘图区。

## [0.30.3] - 2026-09-23

### 修复

- **高亮标记环不再画到坐标轴带上**（`audit:interactions` 的 `ink-over-right-axis` 根因）。
  标记是以**数据点为中心**画的，而滑动窗口 / 缩放之后最常见的状态就是「当前点正好落在
  绘图区边界上」—— 环半径那几个像素必然落进轴带。实测在 40px 宽的轴带里量到 44~58 个
  饱和像素（就是半颗环），而且**不会自愈**，要等下一次更大范围的交互把它盖掉。
  `dashboard` / `dashboard-logistics` 两页因此 3/3 稳定失败。

  做法：`Highlight` 新增 `clipBox`（画布坐标，与 `item.x / item.y` 同一个坐标系），
  由图表在 `syncComponents` 里按绘图区矩形交进来，`doRender` 里 `rect + clip`。
  极坐标 / 雷达 / 仪表盘没有「绘图区矩形」语义，不裁（饼图的环本来就在圆外）。
  `setClipBox` 只在盒子真的变了才置脏 —— 否则每帧重建都会把高亮层标脏，
  局部重绘（脏矩形就是那一小圈环）的收益就没了。

  验证：`audit:interactions` **全量 322 步 ×2 遍 0 问题**（修复前 3/3 失败），
  `PAGES=dashboard,dashboard-logistics` 再连跑 3 遍 0 问题；`audit:hover`（退出码 0、
  failures 空）、`verify:full`（486 单测 + 37 e2e）全绿。截图人工核对过：环被干净地切在
  绘图区边界上，圆点与提示框照常。

  ⚠️ 排查过程里两个**错误猜测**先被证伪，记下来免得下次重走：① 不是「布局几何变化后的
  旧帧残留」（照此改了图表层、跑审计不自洽，已撤）；② 不是「采样采在动画中间」
  （把采样等待拉到 600 / 1200ms，失败只是换了个步骤，照样出现）。
  定位靠的是给审计加的三个入口（`PAGES=` / `INK=1` / `STEP_WAIT=`）+ 逐张截图放大比对。

### 性能

- **轴的刻度表：热路径只走「要画的那几颗」**。0.30.2 之后 10 万根窗口还剩 1.6 ms/tick，
  函数级 profile 已经摊平（`computeLayout` 16% / `syncComponents` 14% / `buildAxisLayout` 12% …），
  说明剩下的不是某个热点，而是**「10 万类目这个长度被反复搬运」**：临时比例尺 `slice()`、
  `ticks()` 再 `slice()`、抽稀的抹白循环、`grid.xTicks` 的 filter、`Axis` 遍历整表 ——
  每处都是 O(n) 的小常数，加起来就是一整块。

  做法（**不动契约**）：`AxisLayout` 增加 `visible: number[]` = 抽稀之后仍要画的原下标。
  `Axis` 与网格竖线都改成按它遍历；惰性形态的抹白只处理**样本**（≤64 颗）而不是整张表。
  `ticks` / `labels` 的长度与下标对齐关系**保持原样**（`Axis` 靠
  `labels.length === ticks.length` 判断「这张表抽稀过」、用例靠 `ticks.length === 类目数`
  断言整表 —— 这两条都没动，所以 **486 个用例一行没改就全绿**）。没抽稀的轴（刻度本来就少、
  或非直角坐标）`visible` 不下发，走原来的整表路径。

  实测（`scripts/measure-pipeline.mjs`，真机 Chromium）：

  | 口径（惰性原始点 + 环形追加） | 0.30.2 | 本次 |
  |---|---|---|
  | 1 万根窗口 每 tick | 0.4 ms | **0.2 ms** |
  | 10 万根窗口 每 tick | 1.6 ms | **0.6 ms**（流水线 0.56） |

  等价性核对（40 / 900 / 20 000 个类目）：`visible` 是真子集（14 / 17 / 16 项），
  「非空标签数 = 真画出的刻度数 = 网格竖线数」三者一致，系列像素非空，
  **与「扫整表」参照逐项比对 0 项不一致**（含每颗的位置）。

  ⚠️ **同时更正一条我一直报错的结论**：`audit:interactions` 并不是「全绿」。
  做这次改动时发现它时好时坏，于是拿**未含本次改动的基线**连跑 3 次 —— **3/3 全部失败**
  （`dashboard` / `dashboard-logistics` 的 `ink-over-right-axis(49~58)`，都在
  legend-toggle / keyboard 这类「布局几何变了」的步骤上）。也就是说**这条门禁在 0.30.2
  的 main 上就是红的，与本次改动无关**；之前我报的「322 步 0 问题」是撞上了通过的几次。
  它与脚本里已登记的 `editable-chart` 已知问题是**同一类**（布局变化后标记层旧位置残留、
  缺一条「布局变化 → 整帧重绘」的正规路径），待单独立项。

## [0.30.2] - 2026-09-23

### 性能

- **类目轴的「类目域 + 类目索引表」不再每帧重建 —— 滚动窗口每 tick 的地板砍掉约 8 成**。
  这是 `plans/incremental-pipeline.md` 那条线的第一刀，但它落在的**不是**施工图原先排的 P1（布局复用）：
  先按函数级 CPU profile 拆了一遍（10 万根窗口、默认示例页宿主），
  `buildXDomain` 33% + `normalizeOption` 自己 25% + `BandScale.indices` 17% —— 八成在地板上游的类目域，
  布局那一段（`computeLayout` + `buildAxisLayout`）只有 4%。按数据把优先级改成「先收类目域」。

  三处改动（都不改语义）：
  ① `rebuild()` **不再把「全量域」当视窗传下去**：原来 `xDomain` 回退成 `this.fullXDomain`，
     于是每帧都让归一化走一趟「把窗口端点映射回类目下标」——`fullDomain.indexOf()` 两趟 O(n)，
     而 10 万类目的滚动窗口里结束端点在最末、起始端点往往已被淘汰，两趟都得扫到底，
     结论还是"退化为原域"。现在只有真有视窗（`dataZoom` / 手势）才传窗口，语义完全一致；
  ② `buildXDomain()` 对**单一惰性原始点来源**走快路径：存储里那张表本来就是「去重 + 首次出现顺序」
     且增量维护的，再走一遍 `seen` 去重等于每 tick 二十万次 `String()` 哈希（复制一份 memcpy 即可）；
  ③ `BandScale` 新增**现成查表口**（`CreateScaleOptions.categoryLookup`）：存储把「类目 → 下标」做成
     O(1) 查询（表里存**绝对序号**，下标 = 序号 − 首项序号，于是头部淘汰只删一个 key、不必重写整张表）。
     表只在「域原样来自那张表、没被视窗裁剪」时才交出去，`resolveXDomain` 一裁就自动退回自建。

  实测（`scripts/measure-pipeline.mjs`，真机 Chromium，默认示例页当宿主）：

  | 口径（惰性原始点 + 环形追加） | 改动前 | ①+② | ①+②+③ |
  |---|---|---|---|
  | 1 万根窗口 每 tick | 1.4 ms | 0.6 ms | **0.4 ms** |
  | 10 万根窗口 每 tick | 15.0 ms | 4.7 ms | **2.0 ms** |

  「普通（concat + `setData`）」那条对照路不变（每 tick 要物化整窗的点，2.1 / 20.2 ms）。
  剩下的已经摊平到布局 / 轴 / 组件同步上（见施工图里改过的优先级）。

- **稠密类目轴不再格式化那些注定要被扔掉的标签**。改完上面三处之后 profile 重跑，
  剩下的成本摊在布局那一段（`computeLayout` 17% + `buildAxisLayout` 13% + `formatTick` 12%
  + `thinXAxisLabels` 7%），再往下拆发现大头不是「没复用布局」，而是**每帧为 10 万个类目
  格式化 10 万个标签、然后把其中 99.9% 抹掉**：类目轴是「一个数据点一个刻度」，
  而抽稀之后真正画得出来的只有十几颗。

  做法：刻度数 > 512 的轴（类目轴才会这么密）**只格式化量宽度要用的样本**（≤ 64 颗 + 首末），
  其余位置留空；等 `thinXAxisLabels` 把抽稀定稿之后，再按保留下来的下标现算
  （`AxisLayout.formatLabel` / `sampledLabelWidth`）。数值 / 时间轴刻度本来就少，语义不变。
  `labels` 数组的长度照旧等于刻度数 —— `Axis` 靠 `labels.length === ticks.length` 判断
  「这张表抽稀过」这条契约没动。

  实测：10 万根窗口 **2.0 ms → 1.6 ms**（1 万根窗口 0.4 ms 不变，它不到 512 的门槛）。
  另做了一次专项核对（30 / 400 / 5 000 / 50 000 个类目）：标签数组长度不变、留白的下标
  就是抽稀步长的整数倍、**每一颗非空标签都等于它自己那一颗的 `formatTick` 结果（0 项不一致）**
  —— 这条是最怕静默出错的地方（标签少画 / 画错都没人会当场发现）。
  回归：`verify:full`（485 单测 + 37 e2e）、`audit:interactions`（322 步 0 问题）、`audit:hover` 全绿；
  新增 `tests/option/raw-category-index.test.ts` 锁住「增量索引表 ≡ 在类目数组里找第一次出现」这条不变式
  （含头部淘汰、**中途淘汰**留下的序号空洞、转环重装、以及轴真正走查表口的黑盒断言）。

## [0.30.1] - 2026-09-22

### 修复

- **滚动窗口（环形 / 惰性原始点）的追加还是 O(n)/tick —— 三处通用修法**。
  调用方（ice-trading-chart 的大窗口 K 线流）实测：10 万根窗口每 tick 追加 4.3ms，
  其中 4.0ms 在归一化 / 布局这条流水线上。三处一起改：
  ① `SeriesRawPoints` **增量维护 x 类目表**（`categories` + 计数）：类目轴每次归一化都要这张表，
     而滚动窗口只动两头 —— 不增量维护就是每 tick 两趟 O(n)；
  ② 取点分成两支：热循环用 `readGenericPointInto` + 复用 scratch（**不分配**），
     `readGenericPoint` 保留给冷路径 —— 之前重算一次域要 new 一万个对象；
  ③ 环形的数据域改**增量更新**（只有淘汰掉当前极值时才重扫全量），
     类目轴上标签宽度也**按样本量**（`measureText` 是真在 ctx 上量，全量量一遍是 O(类目数)；
     首末两颗一定量，避免端点标签更宽时低估）。
  实测（10 万根窗口，真机 Chromium）：环形写入 + 域的**数据路径 4.26ms → 0.13ms**；
  端到端每 tick（含归一化 / 布局 / 同步组件的流水线）4.3ms → 1.7ms；
  对照的「全量 setData」也在同一量级的流水线上，但要多物化 1 万个 `DataPoint`
  （堆 9.1MB vs 5.9MB）。

  剩下的地板是「数据一变就重跑 normalize + layout」这条流水线（~1.6ms/次）——
  要再往下压得让它支持**增量归一化 / 增量布局**，那是另一件事，别把它记成这次的欠账。

## [0.30.0] - 2026-09-22

### 新增

- **自定义系列也能开列存（`virtual: true` 对任意类型成立）**。以前 `virtual` 只认
  `scatter / line / area`（数值列）与 `heatmap`（稠密矩阵），其余类型直接报错 ——
  于是注册进来的自定义系列（K 线那一类）只能按「每点一个 `DataPoint`」走。
  现在多了一类存储：**惰性原始点**（`SeriesRawPoints`）。

  取舍写在契约里：**原始数据按引用保留**（组件按自己的字段解析、提示框照旧拿得到
  `params.data`），省掉的是「每点一个 `DataPoint` 对象」；取点规则与普通系列**共用**
  `readGenericPoint`（两边的 `xValue / y / name / size` 必须逐字一致）。
  **像素缓存照旧建** —— 自定义系列的 `doRender` 通常直接读 `this.pixels`，不建会让它静默不画；
  数值列 / 矩阵才同时省像素与动画缓存。

  追加也通了：`appendData(id, items)` 不给窗口就**原地 push**（调用方那个数组跟着长），
  给了 `maxPoints` 就转成**原始环**（`capacity` / `start`，滚动窗口 O(1)/次，
  不用 `shift()` 搬 10 万个元素；绕回之后逻辑顺序由访问器保证）。
  快照语义与数值列一致：普通形态数据在 option 里（**能进快照**），
  转成环之后归存储所有 → `restore()` 显式报错，而不是悄悄给一张空图。

  回归：`tests/chart/virtual-custom-series.test.ts`（自定义系列端到端：渲染 / 命中 /
  两种追加 / 环的绕回 / 快照）、`tests/option/normalize.test.ts` 的「惰性原始点」一组。

## [0.29.1] - 2026-09-22

### 修复

- **类目轴上万时坐标轴每帧 O(n²)**：`Axis.tickEntries()` 原来对**每一颗刻度**都调
  `scale.map()` 与 `formatTick()`，而类目轴（`BandScale`）的 `map` 内部是
  `domain.indexOf(value)` 线性查类目 —— 10 万个类目时 `axisX` 一次渲染 **3.7s**
  （同一帧里 K 线组件只要 15ms）。两处通用修法：
  ① 抽稀掉的刻度（`labels[i] === ''`）**根本不进表**：画不出来的刻度既不需要位置、
     也不需要标签文本（网格线只看抽稀后的那批 `xTicks`）；
  ② `BandScale` 给「类目 → 下标」加一张**按域数组身份失效**的查表：`indexOf` 从 O(n) 变 O(1)。
     语义不变（先出现者胜；对象类目仍按身份找；不允许「把数值当类目下标」的退化仍在）。
  实测（10 万根 K 线，真机 Chromium）：首帧 `axisX` **3716ms → 0ms**、整帧 **3737ms → 17ms**。
  调用方（ice-trading-chart）的 K 线列存改造配合这条修复，把 10 万根的命中从 223ms 降到 0.00ms、
  首帧从 7.4s 降到 17ms。

## [0.29.0] - 2026-09-22

> **列存（虚拟）系列**：百万级 / 亿级数据的一条完整路线 —— 散点、折线、面积、热力图、
> 实时流环形缓冲、分块按需加载，以及把列交给引擎的虚拟子源。
> 机制、契约、实测与可复用边界见 `docs/10-column-store-virtual-series.md`；
> 改动纪律见 `AGENTS.md` 的「虚拟（列存）系列」一节。

### 新增

- **虚拟（列存）系列：`virtual: true` 的散点 / 折线 / 面积 / 热力图不再建「每点一个对象」**。100 万点原来要
  1M 个 `DataPoint` + 1M 个原始数据项，堆 125MB、命中 0.8ms；现在图表只保留
  x / y（/ size）几条列，读点时才按需合成 —— 同一把尺子实测 **堆 125.2MB → 2.2MB、
  命中 p50 0.8ms → 0.00ms**，平移维持 60fps（16.7ms/帧，没有回退）。
  大数据推荐直接给列：`{ type: 'scatter', virtual: true, data: { x: Float64Array, y: Float64Array } }`
  （`Float64Array` 会被**直接采用**，不再复制一份）。
  代价都是显式的，不静默降级：**不进 option 快照**（`restore()` 会报错而不是还你一张空图）、
  提示框的 `params.data` 为空、`appendData` 报错（改用 `setData`）、只支持数值型 x 的散点
  （类目轴 / 堆叠 / 其它类型一律报错）。折线 / 面积走**同一套列与同一个虚拟内核**：
  折线大窗口下按像素列保留「首 / 最低 / 最高 / 末」四点，尖峰不会被采样吃掉
  （100 万点折线实测堆 125.8MB → 2.6MB、命中 0.8ms → 0.00ms）。
  **热力图走的是「稠密矩阵」**：`data: { xCategories, yCategories, values }`（行优先值矩阵，
  `Float64Array` 直接采用），命中退化成**类目查表 + 下标运算（O(1)）**，亚像素时按屏幕像素块
  聚合绘制（块内取最大值，热点不被抹平）。实测 200×200：命中 6.8ms → **0.00ms**、堆 7.0MB → 2.9MB；
  1000×1000 = 100 万格时堆 3.3MB、命中 0.00ms（普通路 100 万格跑不完：逐格线性 + 每格线性查类目）。
  稀疏数据不要开（归一化按密度报错，避免矩阵比点集还费）。
  示例见 `examples/large-data-virtual-series.html`（散点与折线共用同一份列）与
  `examples/heatmap.html`（第二张是 100 万格矩阵）。
- **亿级数据：分块按需加载（`virtual: true` + `data: { sizes, rangeOf, yDomain, loadChunk }`）**。
  图表只让「可见窗口覆盖的块」驻留（`maxResidentChunks`，LRU 淘汰），其余块可以根本不在内存里 ——
  **内存与数据总量解耦**。实测 10 亿逻辑点（1 万块 × 10 万点、常驻 3 块）：
  初始化 **3ms**、堆 **2.7MB**、数据点对象 0；平移到 9 亿点处换一批块，常驻数与堆不变。
  `rangeOf` 与 `yDomain` 是**声明式必需**：前者让窗口定位不必先加载，后者让坐标轴不随加载漂移。
  实现上踩到并修掉两个坑：① 窗口覆盖的块远多于驻留预算时**不能对窗口内每块都发请求**
  （10 亿点全量视图覆盖 1 万块，全请求 = 1 万次加载 / 初始化 22 秒；改成按预算均匀取样后 3 次 / 3ms）；
  ② 分块系列的**最近邻不能在全量下标上二分**（未驻留区间的 x 是 undefined，会一路走到头、命中判空），
  改成「先用 x 值定位到块，再在驻留块内二分」。
- **把列交给引擎的虚拟子源**：`chart.createVirtualSource(seriesId)` 返回引擎的
  `VirtualChildSource`（`boxAt` / `forEachInBox` / `hitTest` / `paint` / `materialize`），
  坐标是组件本地（= 绘图区）像素，把 `ICEVirtualLayer` 摆在绘图区上即可 ——
  数据**不复制**，窗口裁剪 / 批量落墨 / 命中都归引擎，并白拿引擎侧的能力：
  **命中即物化**（点一下就变成可拖的真图元）、SVG 导出、worker 镜像告警、对齐参考线。
  实测 100 万点散点：落墨 **2.1ms/帧**、命中 **0.00ms**、堆 **2.1MB**。
  示例见 `examples/large-data-virtual-series.html` 的第三张图。
- **虚拟（列存）系列的实时流：`appendData` 走环形缓冲**。以前每次追加都要
  「concat 成新数组 → 整条重跑归一化 / 布局 / 重建像素」，成本随窗口线性长；
  现在容量 = 滑动窗口（`maxPoints`），满了覆盖最老的，每次追加就是
  「写 1 个槽位 + 挪一次起点」。实测同一个 API、单系列逐点追加（p50 / p95）：
  窗口 1500 → **0.10 / 0.40ms**、2 万 → 0.20 / 0.50ms、**10 万 → 0.40 / 0.70ms**；
  普通路径同口径是 0.30 / 0.90、0.80 / 2.90、**5.80 / 15.10ms**（10 万点时一帧预算就没了）。
  于是 60Hz 实时流的可用窗口从「几千点」抬到「十万点」，且**不建数据点对象、不建像素缓存**。
  两条边界：只有数值列系列能流式（热力图是矩阵，追加会显式报错）；环形缓冲的物理下标 ≠
  逻辑下标，读取一律走访问器。示例见 `examples/live-stream.html`（第二张图，窗口 2 万点）。
- **冷路径不再全量扫**（同一条思路铺到消费方）：① 交互窗口的「至少盖住 2 个点」兜底
  原来把全部 x 值去重排序 —— 100 万点每滚一次轮 **63.9ms**，现在在单调列上二分，
  降到 **10.8ms**（剩下的是同步重渲染）；② 无障碍数据表默认封顶 200 行
  （`getDataTable({ maxTableRows })`），超限按等步长抽样并在 caption 里写明
  「共 N 点、已抽样 M 行」—— 少给内容必须说出来。
- **垂直网格线 `grid.x` 落在标签正下方（跟 x 轴抽稀同一批位置）**：以前打开 `grid.x` 是
  **每个刻度一条**竖线 —— 而类目轴的 `scale.ticks()` 会返回整个 domain，视窗里 120 根就是
  120 条线，画出来是一片栅栏（实测截图确认）。现在网格读的是 x 轴布局表里**真画出来的那几颗**
  （`labels[i] === ''` 的不要），于是竖线数量 = 标签数量、位置与标签严格一致；数值轴同理
  （以前网格固定按 `ticks(5)` 画，和轴自己的 `tickCount` 可能对不上）。
  配套一条：**`xAxis.show: false` 的图也出这张抽稀表**（只是不占排版空间、Axis 也不画）——
  多 pane 场景里上面几块 pane 常常藏掉 x 轴省纵向空间，网格却要和下面那块**对齐**成方格，
  没有这张表就只能退回「每个类目一条线」。回归用例：`tests/chart/grid-x.test.ts`。
- **缩放比例限制**（类目 / 时间轴）：新增 `interaction.zoom.minBarSpacing` / `maxBarSpacing`，
  口径是「**每根占多少像素**」（主流看盘软件与主流轻量图表库的 `barSpacing` 口径），
  默认 `0.5` px/根 与 `0`（= 自动，取绘图区宽度的一半，等价于「一屏最少两根」）。
  早先是「占数据域的 5%~100%」：会随「图上载入了多少根」漂移，实测 K 线页缩到底能到
  **0.24px/根**（一根占不到一个像素，整片糊成色带），放大那头则卡在数据域 5%（载入根数一变
  限制就跟着变）。限制对**手势缩放**（滚轮 / 双指 / 框选缩放到区间）生效；程序化 `setDomain`
  与联动回显不受限 —— 那是应用自己的窗口，必须指哪打哪。
  `zoom.minSpan` / `maxSpan` 保留给**连续轴**（数值轴），并补上了文档（以前是没写进类型
  的隐式选项）。实现见 `util/zoomLimit.ts`，回归用例 `tests/interaction/zoom-limit.test.ts`。

### 修复

- **x 轴标签在高密度下糊成一条色带**：两处叠加的毛病。
  ① `Axis` 组件自己又算了一遍抽稀步长（`buildAxisLayout` 已经算过一张表），而它给间距兜了
  一个 `Math.max(1, slot)` 的地板 —— 间距 0.24px 时按 1px 算，步长从「380 根一跳」变成
  「88 根一跳」，79.5px 宽的标签按 20.7px 的间隔画出去（实测 3565 根时画了 **42 个**标签，
  布局那边只留 11 个）。现在抽稀只剩 `thinXAxisLabels` 一处，组件照着画。
  ② 抽稀要按**绘图区宽度**算，但比例尺是在 `computeLayout` 开头用**画布宽度**建的（那时
  y 轴占位还没定），可用宽度多算了 7%~11%（正好是 y 轴那一截），两个标签刚好贴住；
  现在抽稀挪到绘图区算完之后。
  顺带把首末标签也管住：**放不下就整颗丢掉**，不往里推（推右会压住邻居，实测左端标签被推
  27px 正好盖住第二个标签的开头）。回归用例：`tests/components/axis-labels.test.ts`、
  `tests/layout/layout.test.ts`。

- **类目轴的窗口越出数据时被「放大成整段」**：端点 key 不在这份数据里时，
  `clampAxisDomain` 直接退化成了 `from = 0` / `to = length-1`，也就是**整段数据**。
  多 pane 联动时非常致命：某一格的系列类目比别人短（尾巴上有一段没有数据的空档），
  联动过来的窗口在那里找不到右端 key，于是那格当场被拉成整幅（实测：K 线图拖到最新
  数据右边，量图被拉成整幅）。现在：只有一端越界 → **整窗贴着数据边缘滑，跨度按当前
  窗口保持不变**；两端都越界（窗口表达不出来）→ **保持原窗口**，不再跳到整段。
  回归用例：`clamps a category window that runs past the data`。
- **纵向平移方向反了、而且退化成缩放**：
  `shiftDomain` 对 x / y 用同一个符号，但 y 轴的「值越大越靠上」与屏幕反向，
  向下拖会让内容往上跑；另外 `clampAxisDomain` 把窗口与数据范围求交（窗口本来就等于
  数据范围时），平移会被夹成缩放（实测 K 线页纵向拖 120px 量程 `[41100,41700]` →
  `[41100,41449]`）。现在按轴给符号，并让 `source === 'pan'` 单独走「保住跨度、
  只要求与数据范围至少交叠 1/4 个窗口」的分支。
  回归用例：`pans the y domain downward when dragging down`。
- **类目轴上的横向平移整条失效**（数值轴不受影响）：
  取当前 x 窗口时要求 `norm.xAxis.domain.length === 2`，但类目轴的 `domain` 是
  **窗口内的整串类目**（窗口 120 根就是 120 项），于是窗口取成 null、平移被跳过；
  位移计算里又把窗口内那串当成了「全部类目」，算出来的位移恒为 0。
  两处都改成按轴类型取窗口 / 用 `fullDomain` 当全集。表现本来是「能上下拖、不能左右拖」，
  K 线图正是类目轴。回归用例：`pans the domain on a category axis`。
- **联动回显的悬停被"回显之死"清掉**：`linkCharts` 会把悬停回显给同组其它图，而那些图
  本身没有指针悬停；同一次 mousemove 派发到它们时会判定"指针已离开画布"并抛出
  `item:leave`，以前这会顺着联动把源头那张图**真正**的悬停一起清掉。现在只有源头图
  自己发的 `item:leave` 才会清别人。
- **指针不该收走外部放上来的悬停**：`handlePointerMove` 在"指针不在我画布上"时会清悬停，
  但被联动回显的图也是这个状态，于是回显会在同一次 mousemove 里自灭。
  新增 `InteractionController.externalHover` 区分"我自己的指针悬停"与"外部按 x 值
  放上来的悬停"，后者由放它的那一方收回（`showHoverAtValue` / `clearHover`）。
  两个修复合起来，多 pane 的十字准星竖线才能在整摞图上同步出现。

### 新增

- `interaction.hover.mark`（默认 `true`）：悬停时是否在数据点上画标记（圆环 / 柱形描边）。
  关掉它适用于只做十字准星的看盘场景 —— 标记是半透明白填充 + 彩色描边，`mode: 'nearest-x'`
  时同一列里每个系列各画一个，落在蜡烛上还会遮住正要看的那一根。

## [0.28.0] - 2026-09-21

### 变更（破坏性：移除内置 `candlestick` 系列与 OHLC 数据模型）

金融交易类图表不属于本包的职责范围，已整体迁出到同族新包 **`ice-trading-chart`**。
本包不再承载任何交易语义，破坏性点如下：

- **删除内置系列类型 `candlestick`**：`BuiltinSeriesType`、系列注册表、公开导出
  `CandlestickSeries` 全部移除。`series[].type: 'candlestick'` 在本包会**静默降级为折线**
  （未注册类型兜底 `line`），要画 K 线请装 `ice-trading-chart` 并调用它的注册入口。
- **删除 `DataPoint.ohlc`**：OHLC 不再是本包的数据模型概念。需要多值形态（影线极值之类）
  的自定义系列，从 `DataPoint.raw` 自行解析原始数据项，并用显式 `yAxis.domain` 覆盖量程。
- **删除 `SeriesOption.candle`**：涨跌配色改为自定义系列自己的 option 字段
  （`InternalSeries.option` 按引用透传，未知字段会原样到达系列组件）。
- **删除提示框里硬编码的「开盘 / 收盘 / 最低 / 最高」四行**：需要定制提示框行的自定义系列，
  用公开的 `tooltip.formatter` 逃生舱承接。
- 摘除 `normalize` 中与 K 线绑定的三处：`buildPoints` 的 OHLC 分支、
  `resolveXAxisType` 的类目轴推断、`buildYDomain` 的影线极值入域。
- 无障碍 `SERIES_ROLE_HINT` 去掉 `candlestick` 条目。

> 序列化契约不变（`SNAPSHOT_VERSION` 仍为 1，快照只存 option）。含
> `type: 'candlestick'` 的旧快照可以正常读入，但会按上面说的降级为折线。

## [0.27.1] - 2026-09-19

### 修复

- **删掉"只对原始 DOM 事件调 `preventDefault`"的历史绕过**：那是为 `ice-render@2.17` 及以前
  写的 —— 那时 `ICEEvent.preventDefault()` 是 `throw new Error('Method not implemented.')` 的桩，
  直接调会把整条事件派发链打断。2.18 起它是真实现（`cancelable` 时置 `defaultPrevented`
  并**转给原始 DOM 事件**），本包 peer 下限也已抬到 `^2.18.0`，所以改成：
  ① 优先调事件自身的 `preventDefault()`；② 只有"事件桩（仅 `originalEvent`）"或 ① 抛了
  （原生方法未绑定 / 被动监听器）才退回直接调原始事件。

  **对外无行为变化**（回归：原有用例继续通过，新增"事件自己有 `preventDefault` 时优先用它"一条）。

## [0.27.0] - 2026-09-19

### 变更

- **peer 下限抬到 `ice-render@^2.18.0`**：那一版是**事件系统改版** —— 事件开始沿组件树冒泡
  （命中组件 → 各级父容器 → 总线），`ICEEvent` 的 `preventDefault/stopPropagation` 等 W3C 方法
  从「调用即抛异常」变成真生效，事件名也做了类型化。本包对引擎事件名的引用已改用
  `ICE_EVENT_NAME_CONSTS` 常量，`preventDefault` 那层「只对原始 DOM 事件调用」的封装暂时保留
  （等宿主引擎基线抬上去后再简化）。

  仍停在 2.14~2.17 的宿主装本版本会得到 npm 的 peer 冲突提示 —— 有意的：
  事件语义变了却继续用老引擎，会出现「点了子组件、父容器收不到」或「preventDefault 抛异常」这类
  只在运行时才炸的差异。
## [0.26.0] - 2026-09-19

### 变更

- **peer 下限抬到 `ice-render@^2.17.0`**：那一版把 `zIndex` 的默认值改成 **`'auto'` 哨兵**
  （排序当 0、同层平手按**加入顺序**；显式正数是"应用自己钉的浮层"），补了四个 z 序 API
  （`bringToFront` / `sendToBack` / `moveUp` / `moveDown` —— 只排"可排层"，不会改写应用钉的正数），
  并把**序列化格式升到 v2**（旧文档载入时自动迁移，绘制次序逐项不变）。
  本包按 zIndex 编号子件、抬高浮层的地方已经对齐这套口径。

  仍停在 2.14 / 2.15 / 2.16 的宿主装本版本会得到 npm 的 peer 冲突提示 —— 有意的：
  宁可响亮失败，也不要让它在"新旧两套叠放/序列化语义"之间看着能跑、行为却对不上。

## [0.25.0] - 2026-09-18

### 变更

- **peer 下限抬到 `ice-render@^2.16.0`**：本包画图走的是引擎那一份正交路由，2.16.0 修的是「连线横穿图面里的其他图元」（给排水工艺图实测 24 处 → 0）。

  仍停在 2.14 / 2.15 的宿主装本版本会得到 npm 的 peer 冲突提示 —— 有意的：
  宁可响亮失败，也不要让它悄悄用老路由画出一堆穿线。

## [0.24.0] - 2026-09-18

### 新增

- **`ChartEventName` 补上 `mark:drag` / `mark:dragend`**（此前只有运行时事实、类型里没有）。
  这两个事件一直在 emit（`addMark` 的拖拽通道与 `finishMarkDrag()`），README 的事件表也一直列着，
  却漏在 `ChartEventName` 联合类型与 `ChartEventPayloads` 之外 —— 而 `on(event, fn)` 的签名是宽的，
  于是**任何拿事件名做穷举、或建类型安全事件表的地方都会静默漏掉**（不报错，只是收不到）。
  载荷类型是现有的 `ChartMarkData`。

### 变更

- **主题补丁层**（引擎 2.14 起）：本仓写引擎主题的入口从"直接改实例主题"改成**命名补丁**
  （`ice-chart` 用 `setThemePatch('ice-chart', …)`；`ice-entity-designer` 用 `setThemePatch('ice-designer', …)`）。
  以前两边都写基座 → 与 UI 主题互相覆盖、胜负取决于调用顺序（换 UI 主题会把领域主题抹掉，反之亦然）；
  现在两层互不覆盖、调用顺序无关，且基座换了补丁自动重放。老引擎（<2.14）自动退回原写法。
  - ice-entity-designer 额外：外壳是**从基座派生**的，补丁不会自己变 —— 新增订阅，
    基座变化时按新主题重算一遍（表现：换 UI 主题，设计器的选中框 / 手柄跟着换，不会停在旧主色）。
  - ice-chart 的 `theme:'auto'` 不变：它本来就是"跟随引擎"，不往外推主题。

### 门禁

- 新增 `tests/theme/color-budget.test.ts`：写死色值的**按文件预算棘轮**（只减不增）。
  库这一层以前没有这道门禁，于是"顺手写个写死色值"没人拦。
- `tests/chart/marks.test.ts`：mark 拖拽用例的回调参数从 `any` 收成 `ChartMarkData`，
  并加一条类型层棘轮（`Partial<{ [K in ChartEventName]: ChartEventHandler<K> }>` 里必须能写出
  这两个事件）—— 事件名漏一个，`npm run types:check` 就红。**类型缺口只能靠类型门禁守**，
  运行时用例永远抓不住"能收但没类型"。

## 0.23.4 - 2026-09-15

> 本条目于发布后修正过一次：初版只写了 devDep 对齐、并声明"不含图表层功能变更"，
> 但该版本实际还带上了下面这条 `resize()` 修复（它被一并提交进来了，初版漏写）。
> 已发布的 tarball 不受影响（`files` 只含 `dist/**/*`，CHANGELOG 不进货），
> 修正的只是仓库里的记录。

### 修复

- **`resize()` 不再被画布边框撑大**：尺寸对齐改为委托引擎的 `ICE.fitCanvasToDisplaySize()`
  （ice-render **2.12.0** 起新增的公开方法）。旧实现自己算尺寸，用的是
  `getBoundingClientRect()` 的 **border-box** —— 画布带边框时（示例页的画布正是 1px 边框）
  算出来的尺寸比绘制区大，backing store 与命中矩形一起偏。新实现在引擎里按**内容盒**
  （排除 border / padding）算，且 backing store × dpr、`canvasWidth/Height`、命中矩形与内容盒的
  同步都只有一份实现。这本来就是该方法文档注释里写的意图（"读取 canvas 的内容盒尺寸"），
  此前只是意图、不是实现。

### 变更

- **dev 依赖对齐 `ice-render ^2.12.0`**（peer 范围 `^2.10.0` 不变）。2.12.0 除新增
  `fitCanvasToDisplaySize()` 外，还带上了 2.11.3 的**连线命中语义**修正 ——
  ① 容差不再随线段长度二次放大（旧实现 130px 线段实际容差约 14px、400px 长线约 24px）；
  ② 连线不认领自己端点节点盒子内部的点；③ Visio 正交路由无解时不再抛异常（逐级兜底）。
  图表层用折线画系列，命中测试走的就是这条路径：**靠近线但不在线上**的悬停会比以前更"准"
  （不再被 14~24px 的宽容差误判成"在线上"）。

### 验证

- 新增 `tests/chart/resize.test.ts`（6 例：显式尺寸生效 / 内容盒读取 / 边框与内边距双扣 /
  dpr>1 时 backing store 成倍 / 幂等 / 垃圾尺寸不把画布写坏）。
- 本仓门禁：`npm run verify` 全绿（**41 套件 / 385 用例**）、`test:e2e` **36 个示例页冒烟全过**
  （无 pageerror + 画布有输出）。
- 引擎侧对应版本：ice-render 2.12.0 的 `verify:full` 全绿（140 套件 / 1170 用例、视觉 100/100、
  5 段 bench 达标、`pkg:check` 无问题）。

## 0.23.3 - 2026-09-15

### 变更

- **dev 依赖对齐 `ice-render ^2.11.2`**（peer 范围不变）：2.11.2 修了「对齐引导的候选目标漏掉容器内部的子节点」
  （BPMN 泳道 / 复合状态 / 二次回路端子里的图元拖不出对齐线）。本仓**不启用对齐引导**，
  所以引擎这次改动对图表层是空操作 —— 本版本只为把本地开发与回归固定在 2.11.2 上，**不含功能变更**。
- 门禁（在 2.11.2 上重跑）：单测 40 套件 / 379 用例、36 个示例页冒烟（无 pageerror + 画布有输出）、
  334 步交互审计（`problems: []`）、悬停扫描（hover-sweep 退出码 0）全绿。

## 0.23.2 - 2026-09-15

### 变更

- **peer / dev 依赖对齐 `ice-render ^2.10.0`**（原 `^2.8.0`）：引擎 2.8–2.10 的布局机制、
  分层渲染与动画写值通道都已稳定，本仓在 2.10.1 上重跑过全部门禁 —— 单测 379 通过、
  36 个示例页冒烟、334 步交互审计全绿。本版本不含功能变更。

## 0.23.1 - 2026-09-15

### 修复

- **悬停的两条铁律**（多图同页时切页必现的幽灵提示框）：
  1. **画布量不到尺寸（`canvasWidth/Height` 为 0）＝ 指针不在本图**，`isOverCanvas` 判 `false`。
     引擎的原生监听挂在 **window** 上，同页每张图都会收到整页事件，这个判断是唯一的"事件不属于我"
     闸门；旧实现"尺寸未知就当指针在上面"，于是被 `display:none` 藏起来的图会把全页移动都吃下来、
     在错误坐标锁住悬停 —— 显示出来后 `refreshHover()` 重新解析，画面上凭空多出一个擦不掉的
     提示框 + 十字准星。
  2. **指针离开画布要主动 `setHover(null)`**，不能只"忽略这次事件"：画布外不再产生让本图重新取
     悬停的坐标，只 return 的话提示框会一直挂着。
  - 回归：`tests/interaction/isolation.test.ts` 新增两条（离开画布清悬停 / 未布局的画布不算悬停），
    AGENTS 补「悬停的两条铁律」。

### 依赖

- `ice-render`: `^2.6.0` → `^2.8.0`（布局机制对齐 Swing；peer `^2.0.0` 本就覆盖）。
- 验证：jest 379/379、build、playwright 36/36。

## 0.23.0 - 2026-09-14

本轮主题：**`theme: 'auto'` 真正跟随引擎主题**（引擎 2.6 起广播主题变更），
并顺带把依赖对齐到引擎 2.6.0 的文本行为。

### 新增

- **`theme: 'auto'` 会跟随引擎主题的后续变更**：`ice.setTheme('dark')` 之后图表**立即**重新归一化
  （`auto` 的明暗重新判定）并重绘，不用等下一次 `setOption`。
  - 订阅 `ice.onThemeChange(...)`（引擎 2.6 新增），`destroy()` 时退订；
  - 只看 `kind: 'theme'`（交互外壳变化不影响图表配色），且**只在当前 `option.theme === 'auto'` 时**重建；
  - 显式主题（`'light'` / `'dark'` / 自定义片段）**不跟随**，也不会反过来覆盖宿主刚设的主题。
  - 此前 `auto` 只在**建图 / setOption 那一刻采样一次**引擎明暗：宿主之后换主题，图表的轴 / 系列 / 图例
    全是旧配色纹丝不动 —— 与文档承诺的「跟随 ice-render 实例主题」不符。

### 依赖

- `ice-render`: `^2.5.1` → `^2.6.0`（peer 范围 `^2.0.0` 本就覆盖，devDependency 提到最新）。
  引擎 2.6.0 顺带修掉了「文本放不下就被 canvas 压扁字形」的老问题（改成截断 + 省略号）——
  图表里的轴标签 / 图例 / 数据标签因此不会再被压变形。

### 验证

- `verify:full` 全绿（377 单测，含 4 条新增的「auto 跟随」用例；36 个示例页浏览器冒烟）。

## 0.22.0 - 2026-09-14

本轮主题：**数据系列配色收敛到引擎的家族色板**（对齐 `ice-render@2.5` 的品牌基线），
顺手修掉一个「写在顶层的瀑布图配色不生效」的真 bug。

### 变更（破坏性：默认系列配色）

- **`colorPalette` 不再由本库自行定义**，改为引引擎的家族色板：
  亮色 = `FAMILY_PALETTE`，暗色 = `FAMILY_PALETTE_DARK`（都从 `ice-render` import）。
  以前家族里有两份 8 色（引擎一份、本库一份），**同一份数据用引擎画、用图表画，从第 2 个系列起
  颜色就不一样**；现在只有一份，`ice-render` 是它的家。
- 因此**默认系列配色的顺序会变**：亮色主题此前走「Bootstrap 语义色序」
  （primary → success → danger → info → indigo → warning → orange → teal → pink → secondary，共 10 色），
  现在统一成家族色板（蓝 → 绿 → 琥珀 → 红 → 紫 → 青 → 粉 → 靛，共 8 色）。
  暗色主题同样收敛，**少掉原先的第 9/10 色**（多余的粉与灰），超出后按色板循环。
  显式传 `theme: { colorPalette: [...] }` 或给系列 / 数据项指定 `color` 的用法完全不受影响。
- `CHART_PALETTE`（layout/force、layout/treemap、layout/sankey、WaterfallSeries 的兜底）
  现在就是家族色板的副本 —— 以前它与主题里的 `colorPalette` 是两份不同的 8 色，
  同一页面里不同类型图表可能来自不同色板。

### 修复

- **顶层 `option.waterfall` 一直不生效**。types 里承诺「`option.waterfall` 与系列级 `waterfall`
  两者等价，series 优先」，但绘制侧只读了系列级，写在顶层的 `increaseColor / decreaseColor /
  totalColor / connector` 全部静默失效。旧测试之所以"通过"，是因为它断言的
  `#198754 / #dc3545 / #0d6efd` 恰好与色板兜底值相同 —— 换了色板才暴露出来。
  现在归一化阶段就把两者解析成一份（系列优先），并补了「顶层生效 + 系列覆盖顶层」的单测。

### 验证

- `verify:full` 全绿（单测 + 全部 examples 的浏览器冒烟）。
- 依赖对齐：`devDependency ice-render ^2.5.1`（家族色板从这一版起可从包根 import）。

## 0.21.0 - 2026-09-14

本轮主题：**把图表主题和引擎主题接起来**（引擎 2.4 起），并修正 `theme: 'auto'` 的实现。

### 新增

- **图表主题 → 引擎主题的桥**（`theme/chartEngineBridge.ts`）。本库有自己的 Bootstrap 主题，
  它管的是图表画出来的东西；但一个图表实例里还可能有**引擎自己画的东西** —— 引擎的默认样式、
  交互外壳（选中框 / 手柄 / 插槽 / 引导线 / 连线标签）、以及应用往 `chart.ice` 里塞的自定义图元。
  两套主题不打通就会出现「图表是暗的、引擎那层还是亮的」。
  - `chartThemeToEnginePatch(theme)`：**只用图表主题已有的 token** 按同名语义映射
    （background / text / muted / hint / border / palette / chrome.selection / chrome.linkLabel /
    base.fontFamily / base.fontSize），不新造颜色；
  - `applyChartThemeToEngine(ice, theme)`：一次性应用（语义色 + 外壳）。
  - `ICEChart` 建图与 `setOption` 时自动应用；**主题没变不重复推**（推一次会让引擎整棵树标脏），
    `theme: 'auto'` 时**不推**（auto 是"跟随引擎"，推回去会变成自己跟自己的回喂）。

### 修复

- **`theme: 'auto'` 以前恒等于 `'light'`**（`resolveChartTheme` 里只判断了 `'dark'`），与文档承诺的
  「跟随 ice-render 实例主题」不一致。现在按引擎实例主题 `semantic.background` 的相对亮度判定明暗，
  真正跟随（归一化层仍然保持纯函数：`preferDark` 由 `ICEChart` 算好传进 `NormalizeContext`）。
- `rebuild()` 里那次归一化也要带 `preferDark` —— `this.norm` 来自那一遍（第一遍只是编译管线的前置）。

### 说明：哪些颜色**不进**主题

桑基节点边框、树图顶层标签、折线 / 散点标记的外圈这类**压在饱和色块上的白字与白描边**，
是对比色而不是主题色 —— 换主题也不该变，所以它们仍然是写死的白色（这是有意的，不是漏网）。

### 验证

- 单测 +5（映射原则 / 建图即推 / setOption 跟随 / auto 真正跟随引擎 / 自定义片段）。
- `verify:full` 全绿：**372** 个单测（40 套）+ 36 个浏览器用例。

## 0.20.1 - 2026-09-14

本轮：**对齐并验证引擎 ice-render 2.4.0**（无 API / 行为变更，补丁级）。

- devDependency 从 `^2.3.2` 对齐到 `^2.4.0`（peer 仍是 `^2.0.0` —— 图表层不依赖引擎的主题 API，不用动）。
- 引擎 2.4.0 带三处**观感 / 行为变更**（容器默认透明、叶子默认样式来自主题、`muted`/`hint` 调深），
  逐一确认对本库无影响：
  - 图表的所有组件都显式给 `style`（不走引擎默认样式），容器（plotArea / grid / 轴 / 图例 / 覆盖层）也都显式指定；
  - 本库有自己的调色板与主题解析（`theme/chartTheme.ts` 的 Bootstrap token），不读引擎的 `semantic`；
  - `STYLE_PRESETS` 只读化、主题快照 diff、深合并：本库都没用到（图表快照是 **option 快照**，
    不是引擎组件树快照，与引擎 2.4 新增的 `theme` 字段无关）。
- 门禁：`verify:full` 全绿 —— 367 个单测（39 套）+ 36 个浏览器用例（30 页示例冒烟 + 6 条标注专项）。

## 0.20.0 - 2026-09-14

本轮主角：**标注图层 `option.annotation`**（目标线 / 阈值线 / 异常点 / 目标区间）。
「目标线 / SLA 阈值 / 达标区」这类最高频的诉求以前只能靠新增一种图表类型来解决，现在它是一条配置：
声明式、纯数据、随坐标系一起动，越界不画但给结构化诊断。详见下面第一条。

> **版本归档说明**：再往下的「图表不再是封闭渲染器（三条能力）」**实际是随 0.19.1 一起发布的**
> （0.19.1 当时的段落只写了「对齐引擎并验证」，完整说明一直留在这一段里）。本次没有改动它们，
> 保留原文以便查证；本版真正新增的只有标注能力。

### 新增：标注 `option.annotation`（目标线 / 阈值线 / 异常点 / 目标区间）

「目标线、SLA 阈值、告警线、达标区」是业务里出现频率最高的一类诉求，而它**不是一种新的图表类型**：
它是挂在坐标轴上的一个图层（与网格线 / 准星同族），数据来自 option、几何来自比例尺。

```ts
annotation: {
  lines: [
    { axis: 'y', value: 3200, text: '目标 3200' },                 // 水平目标线
    { axis: 'x', value: '6-18', text: '上线' },                     // 垂直线
    { axis: 'y', value: 1500, text: '阈值', dashed: false },        // 实线 / 自定义虚实
  ],
  points: [{ x: '6-14', y: 640, text: '异常点', symbol: 'diamond' }],
  areas: [{ axis: 'y', from: 0, to: 1000, text: '达标区' }],
}
```

- **定位走比例尺**：数值轴按数值、类目轴按类目名或下标、时间轴按时间戳 / 日期串；
  因此随缩放、平移、跨图联动一起动，缩放窗口外的标注自动消失、缩回来又出现。
- **不进图例、不占数据下标**：不参与堆叠、不污染提示框的数据行，也不吃命中
  （压在数据点上时点到的仍然是数据）。
- **越界不画**（而不是裁成半条），原因进 **`chart.annotationDiagnostics()`**：
  值写错 / 类目不存在是 `error`（表单标红），越界或场景没有直角坐标系是 `warning`。
  单条坏标注不影响其它标注，也不让图表崩 —— 与表达式诊断同一套契约。
- 纯数据、可序列化：跟着 `toJSON()` 快照一起存盘还原，往返后像素一致。
- 图层位置在**系列之上、坐标轴与覆盖层之下**；文字用主题的 `labelHaloColor` 描边
  （标注常常压在数据线上，没有描边会糊在一起）。

新增 `annotation/resolve.ts`（纯函数：数据 → 像素 + 诊断）、`components/Annotation.ts`（绘制层）、
`normalizeAnnotation()`、`chart.annotationDiagnostics()` / `annotationErrors()`。
示例：[examples/annotation.html](./examples/annotation.html)。

**验收**：单测 14 条（数值 / 类目 / 下标 / 时间轴定位、横向排布、多 y 轴 axisIndex、越界、非法值、缩放跟随、
区间夹取、不参与命中、非直角场景、序列化往返）；真浏览器 6 条（真光栅化、墨迹不越出画布、
**除标注覆盖区域外与「无标注」逐像素一致**、越界诊断、缩放后消失与恢复、
**存盘 → 载入 → 再出图逐像素一致**、**布局变化后旧位置不留残墨**）。
实测：39 套件 / **367 用例**，示例页冒烟 30 页 + 交互审计 334 步 + 悬停扫描全过。

### 新增：图表不再是封闭渲染器（三条能力）

**1. 自定义系列类型 `registerSeriesType(type, factory)`**
任何继承 `SeriesBase` 的子类都能接入成一等系列：拿到归一化后的数据点与直角坐标比例尺，
命中判定 / 悬停高亮 / 提示框 / 图例 / 无障碍 / 快照序列化自动生效。
内置类型不允许覆盖（会破坏 option 的跨端可复现性），未注册类型兜底按折线渲染（不会整张图空白）。
`SeriesType` 也放宽成 `BuiltinSeriesType | (string & Record<never, never>)`，写自定义类型有类型支持。

**2. 数据坐标图元 `chart.addMark()`**
把**引擎图元**挂到数据坐标上：`point` / `xLine` / `yLine` / `xBand` / `yBand`。
组件由调用方创建，所以它天然参与引擎的命中测试、事件与动画；chart 负责按数据坐标摆位、
填充线 / 带的尺寸、越界自动隐藏，并且跟着缩放 / 平移 / 数据更新走（不脱锚）。
配套 `getMark / getMarks / removeMark / clearMarks / isMarkComponent`。

**3. 图元的拖拽语义**
可拖的图元拖完会反解成数据坐标写回锚点，抛 `mark:drag`（实时）与 `mark:dragend`（收尾），
也可以给 `spec.onDrag` 回调。图表层的交互会给图元让路：按在注释 / 阈值线上不会触发框选或平移。

示例：[examples/editable-chart.html](./examples/editable-chart.html)（火花条自定义系列 + 可拖阈值线 + 预测带 + 峰值注释卡片）。

### 已知缺口：SVG 导出

引擎有 `exportSvg()`，但它对**图表**只导得出 13 个 `<rect>`（实测 1400 字节、没有曲线、没有文字）——
因为引擎的导出器基于「路径命令流」（`ICEComponent` 的 Path2D 记录），而图表的系列 / 坐标轴 /
文字都是直接用 ctx 画的。要真正支持 `chart.toSVG()`，需要补一层 **canvas→SVG 记录层**
（把 ctx 的调用翻译成 SVG 元素），或者让所有系列改走记录式路径 API。这条单独立项。

### 已知问题（显式登记，别静默绕过）

**1. 标记层在「布局变化 + 局部重绘」下会残留 ~9px 的线段**
缩放后 y 轴标签可能变宽（`plot.x` / `plot.width` 跟着变），此时标记层在**旧绘图区位置**上留下的
暗红线段不会被脏矩形清掉（实测审计探针报 112 像素越界）。已把图元位置改走引擎的 `setPosition`
（会同时标脏旧位置），绝大多数情况已消失；但「布局变化」这一组合还没根治 ——
试过用引擎的 `renderer.__forceFullRender` 单帧强刷，在这条路径上没生效，所以没有把它留在代码里。
`scripts/audit-interactions.mjs` 用 `KNOWN_ISSUES` 显式登记了这一条（截图照拍、其他断言照跑），
待办是给图表层补一条正规的「布局变化 → 整帧重绘」路径。

**2. SVG 导出对图表还是空的**（见下）

### 测试

- `tests/chart/series-registry.test.ts`（9 条）：注册 / 校验 / 覆盖保护 / 注销兜底 / 自定义系列的命中与悬停 / 序列化往返
- `tests/chart/marks.test.ts`（7 条）：四种图元的摆位与尺寸 / 缩放跟随 / 越界隐藏 / 拖拽回写与事件 / 生命周期
- 实测：38 套件 / **353 用例**


### 新增：数据域留白（绘图区的「padding」）

用户反馈：「图表绘制时要注意绘图区的边界，可绘图区域应该留下适当的空白空间，类似于 CSS 里 padding 的概念。」

量了 46 张示例直角坐标图：**34 张**的系列墨迹贴到了绘图区边缘（距离 < 2px）—— 曲线顶格、K 线影线顶到边框。

现在数值 / 时间轴的数据域默认**上下各留 5%**（按数据跨度算），`xAxis.padding` / `yAxis.padding` 可调，
`padding: 0` 关掉：

```ts
yAxis: { padding: 0.05 }   // 默认值；写 0 表示精确贴边
```

三条边界规则：

- **显式写了 `min` / `max` 的那一侧不动**（用户说了算）；
- **柱形 / 面积被强制包含 0 的那一侧不动**（基线要贴在轴上，抬起来是错的）；
- **log 轴不做**（按对数比例外扩会失真）。

留白必须放在 `niceDomain` **之后**：先留白会把数据最大值 40 抬到 42、再取整变成 50 —— 白多一整格，
而且会让「数据更新时的域过渡」多一次（实测把一条动画用例都弄挂了）。先取整再留白，得到的是「整刻度 + 5% 余量」。

同批把示例里**写死的 y 范围**按实测峰值补了余量（行情 / 能源 / 物流 / 设备 / 函数实验大屏共 9 处：
严重度 0~4 → 0~4.5、机组出力 20~100 → 20~105、叠加函数图 ±1.6 → ±2.2，后者原本已经把峰值裁掉了）。

### 修复：交互过程中曲线会「缩成一团」

用户反馈：「那些曲线在交互过程中不能突然就缩成一团。」

复现到了：**dataZoom 滑块拖到最右**时，窗口会落进「取整出来的空白区」——
`time-series`（30 天逐小时数据）拖到窗口 `[0.98, 1]` 后**可视数据点 0 个**，整条曲线什么都不画（实测墨迹 0×0）。
滚轮缩放有 `minSpan` 兜着，滑块这条路径当时完全没有兜底。

现在交互产生的窗口都会过一遍「至少盖住 **2 个数据点**」的兜底（类目轴是至少 2 个类目）：

- 兜底只作用于**用户手势**（滚轮 / 滑块 / 框选）；
- `setDomainFromFractions()` 作为**纯映射**保持精确（新增第 4 个参数 `guard`，由滑块交互路径传 true），
  程序化控制与 `linkCharts` 联动不受影响；
- 数据只有 2 个点时放宽到 1 个点（本来就画不出线段）。

修复后同一个操作：窗口自动回退到 `[0.897, 1]`，**可视点 2 个**，线段正常。

### 审计升级

- `audit-space.mjs` 新增**留白检查**：自动算出来的数据域不许贴边（显式范围只提醒、不判失败）；
- 审计的度量口径改成「系列数据点的像素包围盒」而不是扫像素 —— 深色主题的**坐标轴线本身就是彩色**
  （`rgba(124,140,255,…)`），扫像素会把它当成系列墨迹，实测产生 -395px 这类假阳性；
- `audit-interactions.mjs` 补上**原本缺失的滑块步骤**（原来 10 步里没有滑块）并断言窗口不会塌缩；
- 等比坐标 / 类目轴的利用率判据按图形族分开（band 内缩半个带宽是几何事实，不是浪费）。

### 测试

- 新增 `tests/option/domain-padding.test.ts`（10 条）：留白生效 / 显式范围不动 / 0 基线不动 / `padding: 0` /
  可调大 / log 不参与 / 滑块与滚轮的兜底 / 程序化映射精确 / 类目轴至少 2 类目；
- 三个既有用例改成断言**意图**而不是写死数值（域含留白后起点不再是 0）。

实测：36 套件 / **337 用例**、交互审计 **312 步 0 问题**、空间审计 **0 失败**（17 条排版提醒）。

## 0.19.1 - 2026-09-14

本轮：**对齐引擎 ice-render 2.3.2 并完成验证**（无 API / 行为变更，补丁级）。

- devDependency 从 `^2.3.0` 对齐到 `^2.3.2`（peer 仍是 `^2.0.0`，不用动）。
- 门禁：`verify:full` 全绿 —— 353 个单测（38 套）+ 29 个浏览器 e2e（含示例页冒烟）。
- 针对引擎 2.3.2 的两处行为改动做了定向验证：
  - **静态层位图对本库不参与**：每个图表实例只有 15~23 个组件，远低于静态层 256 成员门槛，
    实测所有示例页 `__layerBuilds` 恒为 0；能稳定对照的页面在「层开 / 层关」下逐像素一致。
  - **命中测试复用队列**（hover 路径每次 mousemove 都会调）：`scripts/hover-sweep.mjs` 19 种系列逐点悬停
    无报错 / 无残留像素 / 无准星滞后；`scripts/audit-interactions.mjs` 323 步 0 问题。


## 0.19.0

### 新增

- **`option.labels`：内置文案可覆盖**（2026-09-13）：图表包自己不吐文案，但无障碍数据表的
  表头 / 标题、默认 tooltip 里「坐标 / 水位」这类标签是它渲染的 —— 现在这些都能由调用方覆盖
  （`labels: { chart, sector, value, ratio, indicator, coordinate, liquid, slice }`），
  不传时仍是中文默认值。图表包**不内置 i18n 运行时**：应用层用任意 i18n 库把最终字符串传进来
  （或用 `tooltip.formatter` 完全接管提示框）。契约见 ice-render
  `docs/architecture/17-i18n-boundary.md`。

### 测试

- **示例页冒烟回归**（2026-09-13）：`e2e/examples-smoke.spec.ts` 用真实浏览器逐页走
  28 个示例页，断言「无 pageerror / console error」+「每张 canvas 的内容像素占比 > 0.5%」
  （不是"有任意不透明像素"——只刷一层底色的空页也会骗过旧判据），并确认
  `window.ICE` / `window.ICEChart` 都已加载。`npm run test:e2e` 一把过，
  `npm run verify:full` = verify + test:e2e。反向验证过：移走 vendor 里的 UMD 时对应页面如期失败。

## 0.18.0

### 变更（破坏性：快照里的 typeId 换了名字）

- **图元类型标识统一为 `namespace:Type`**（2026-09-13）：9 个图表内部图元的注册名从
  旧 kebab 形式（`ice-plot-area`、`ice-axis`…）改为 canonical typeId
  （`ice-chart:PlotArea`、`ice-chart:Axis`、`ice-chart:GridLines`、`ice-chart:Legend`、
  `ice-chart:Title`、`ice-chart:Tooltip`、`ice-chart:Crosshair`、`ice-chart:Highlight`、
  `ice-chart:Brush`），与引擎内置的 `ice-render:*` 分属不同命名空间。
  同步升级到 **ice-render 2.0.0**（`registerType` 只接受 `namespace:Type`）。
- **不再吞掉重复注册**：旧实现用 `try/catch` 把重复注册的异常吞了；现在重复注册（同一 typeId
  注册不同构造函数）会明确抛错，不会被静默覆盖。
- 旧快照里的旧 kebab 名不再被识别（引擎侧不再兼容无 namespace 的旧名），需要的话按上表改名。

## 0.17.2

### 修复：有些图表没把横向空间用起来（用户反馈「右侧浪费太多」）

先把 104 张示例图全量量了一遍（画布上真实墨迹的横向范围，不是靠眼睛看）：

| 类别 | 数量 | 墨迹平均占宽 | 结论 |
| --- | --- | --- | --- |
| 直角坐标（非等比） | 59 | **96.5%** | 没问题，右侧只留默认 16px 边距 |
| 圆形类（饼 / 雷达 / 仪表盘 / 水位球） | 21 | 45.5%（修前 ~25%） | 一部分是形状限制，一部分是真 bug |
| `aspect: 'equal'` 等比坐标 | 5 | 65.8%（修前 39.7%） | 绘图区被压成正方形居中，两侧死区 |

三个原因，分别处理：

1. **仪表盘 / 水位球被误减了一圈「饼图引导线标签」预留**（引擎 bug）。
   `hasLabels` 把 gauge / liquid 也算进去了，但这两种图的文字全在图形**内部**
   （仪表盘刻度贴弧、数值在圆心；水位球名称与数值在球心），根本不需要外圈。
   实测半径只有可用空间的一半：420×300 的画布里指针盘直径只有 92px。
   现在按可用空间的形状直接算半径（仪表盘横向留 14px 给刻度数字、纵向按半高；水位球整圆贴满较短半轴）。
   结果：仪表盘横向占宽 22% → **53%**，水位球 27% → **40%**。
2. **示例排版把「方」的图形塞进过宽的卡片**。行情大屏的「行业占比」玫瑰图放在 762×170 的面板里，
   圆只占 14% 宽、两边全是死区；函数实验大屏三张等比坐标图放在 498×200 里，只占 35~41%；
   迷你 MATLAB 的极坐标/参数曲线卡片是 960 宽的整行。现在分别收窄到 366 / 3 列 / 并排两列，
   等比坐标的横向利用率 40% → 66%（迷你 MATLAB 那两张到 88%）。
3. **面板栅格类只有 3~8、12 档**（脚手架 bug）。`dk-span9` 没有对应 CSS 时不会报错，
   而是退化成「自动占 1 列」，结果两个面板叠在同一个格子里 —— 这次把「大单成交」改成 9 列时正好踩中
   （截图里散点图直接压在玫瑰图上）。`dash-kit` 现在把 1~12 列全给出来。

顺带修掉一个被放大暴露的既有问题：**仪表盘的读数会压住弧两端的刻度数字**。
270° 表盘的底部本来就是缺口，读数现在按缺口位置摆放（整圆表盘维持原样）。

### 新增：空间利用率审计 `scripts/audit-space.mjs`

把上面这套量测固化成门禁（跑在示例服务上，和另外两个审计脚本一样）：

- 直角坐标（非等比）：墨迹横向占画布 ≥ 85%；
- 刚性圆形（仪表盘 / 水位球 / 非玫瑰饼图）：图形直径 ÷ 该图分配到的直径 ≥ 90%
  ——这条就是这次抓出半径被压小的指标；
- 等比坐标：墨迹横向占画布 ≥ 45%；
- 另出一份「卡片对圆形图偏宽」的**提醒**（不算失败，排版取舍交给人）。

当前 104 张图：**0 项失败，2 条提醒**。

### 修复：悬停实测在大屏上会偶发假失败

探针视口是 1280px，而大屏设计宽是 1572px —— 最右侧那几块面板在视口外，
鼠标移到那里不产生任何事件，探针就报「hoverIndex: null」（表现为随机某张图失败）。
现在视口宽度提到 1700，两次连跑 411 / 414 项全部通过。

## 0.17.1

### 修复：十字准星追不上鼠标（大屏上表现为「竖线飘来飘去」）

用户反馈：「鼠标在图表上移动时，那根表示当前横轴的竖线总是飘来飘去，并不一直跟着鼠标。」

复现量测（Playwright 驱动真实鼠标 + 逐帧采样准星的目标位置 / 绘制位置）：

| 页面 | 跟随时长 | 跳变最大滞后 | 收敛时间 | 扫动平均滞后 |
| --- | --- | --- | --- | --- |
| 静态折线 | 420ms | 490px | 392ms | 75px |
| 大屏（滑动窗口） | 420ms | 409px | **永不收敛** | 122px |
| 类目轴柱形 | 420ms | 357px | 381ms | 56px |

两个原因叠加：

1. **跟随时长取错了源**：`crosshair.followDuration` 原本跟着 `animation.update.duration` 走
   （默认 420ms）。那是「数据变化时图形怎么变」，跟「准星跟指针」是两回事 ——
   420ms 的 easeOutCubic 跟随，肉眼就是线永远落后鼠标一大截。
2. **每次 mousemove 都重启补间**：每次都从当前位置再开一段固定时长的补间，
   指针一动就重新计时，于是永远到不了目标；数据流页面每帧 `refreshHover()` 更让它彻底不收敛。

现在：

- 跟随时长**按距离缩放**（5px/ms，上限 `crosshair.followDuration`，默认 90ms）：
  相邻列这种小位移当帧就落位，只有大跨度跳转才有一段可见的滑动；
- `crosshair.followDuration` 成为独立配置（`0` = 立即跟随），与更新时间动画彻底解耦；
- 目标没变时不再重启补间（数据流页面每帧刷新的场景）。

修复后同一套量测：跟随时长 90ms，收敛 81~98ms，扫动平均滞后 **5~8px**（原 56~122px）。
悬停实测新增「准星跟随」断言（指针停下 220ms 后准星必须落在目标 3px 内），守住这条不再退化。

同批加固：悬停实测的「像素缓存新鲜度」检测器在比对新先 `finishAnimations()` ——
大屏里树图 / 漏斗的 morph 是 520ms，补间推进途中比对会把合法差异误报成「缓存陈旧」（实测偶发一次）。

## 0.17.0

### 发布到 npm（首个公开发布版本）

- **包名必须带作用域**：无作用域名 `ice-chart` 会被 npm 的「相似名保护」直接拒发
  （`E403 Package name too similar to existing package icechart`）——
  同名检查只查「包是否已存在」，相似名检查在发布时才触发，`npm view` 是看不出来的。
  本包因此统一发布在 `@ice-render` 作用域下（与仓库 / 组织同名）。
  想换回无作用域名，只能换一个差异更大的名字（例如加限定词），不能再用 `ice-chart`。
- 版本号从 `0.1.0` 对齐到 **`0.17.0`** —— 之前只发仓库、没发 npm，`package.json` 一直没跟着
  CHANGELOG 走，这次把两边并成一条线。
- 补齐发布元数据：`license: MIT`、`homepage`、`bugs`、描述与关键词（原先只有 `files: dist`）。
- 新增 **`prepublishOnly: npm run verify`**：`dist` 是构建产物（不进 git），
  没有这道闸门就可能把上一次的旧产物甚至空目录发上去。
- 体积分析图从 `dist/stats.html` 挪到 `.stats/bundle.html`：`dist` 是发布内容，
  `files: ["dist/**/*"]` 会把它一起打包（实测白送 187.8 kB 的开发产物）。
- CI 不再克隆引擎源码（引擎已经是 npm 依赖），改成 `npm ci && npm run verify`。
- README 面向 npm 用户重写开头：徽章 / 头图 / 30 秒上手（含 UMD + CDN 写法）/
  交互与序列化截图 / 六个大屏的对比图，截图放在 `docs/screenshots/`（不进 npm 包）。

### 依赖：引擎改为 npm 安装

- `ice-render` 从 `file:../ice-render` 改成 **npm 依赖**（`peerDependencies` + `devDependencies`
  都是 `^1.4.7`），克隆下来 `npm install` 就能跑测试与示例，不再要求把引擎仓库放到同级目录；
  lockfile 里是可校验的 registry tarball。
- 全量验证（用发布版引擎 1.4.7）：**35 套件 / 321 用例**、交互审计 **297 步 0 问题**、
  悬停实测 **414 探针 0 失败**。

### 新增：大屏脚手架 + 三个大屏

- **`examples/assets/dash-kit.js`**：大屏公共部分抽成脚手架 —— 注入共享 CSS（面板 / 四角亮角 /
  KPI 条 / 顶栏 / 底部快照面板）、按 12 列栅格产出面板 HTML、主循环（`tick` + 暂停 + 倍速，
  并把控制器挂到 `window.__dashLoop` 供「像素缓存新鲜度」检测暂停数据流）。
  新增一个大屏的量级因此降到**一个 HTML 文件**，页面里只剩「配色变量 + 面板清单 + 数据怎么动」。
- **`examples/dashboard-energy.html` 能源调度大屏**（蓝紫 · 12 张图）：电网负荷 `appendData`
  滑动窗口并与新能源出力三路联动、电网频率仪表盘、机组负荷率水位球、源→网→荷桑基、
  区域负荷热力、电网拓扑（可拖节点）、电量平衡瀑布、机组出力箱线图、时段电价玫瑰图、
  **谐波合成**（`function` 系列：基波 + 3/5/7 次谐波 ≈ 方波，参数随数据流扫动、曲线原地重采样）、
  机组排名（逐项配色 + 按值排序）。
- **`examples/dashboard-logistics.html` 物流调度大屏**（橙红 · 12 张图）：订单吞吐 / 履约时效
  双图联动、分拣漏斗、在途水位球、仓库容量矩形树、单票成本瀑布、配送网络（可拖节点）、
  包裹流向桑基、时效分布箱线图、承运商排名（逐项配色）、分时揽收（越限时段标红）、异常件散点。
- **`examples/dashboard-lab.html` 函数实验大屏**（紫 · 8 张图 + 表达式诊断面板）：
  五种函数形态同屏、参数 `a/b/c` 扫动（曲线原地变形，不是换图）、李萨如曲线、极坐标玫瑰线
  （花瓣数连续变化）、泰勒逼近、高斯波包、参数曲线族、
  **采样密度对照**（9 点均匀 / 40 点均匀 / 自适应细分）。
  底部是可直接改公式的输入框：改表达式 → 引擎编译 → 报告错误位置 / 未定义变量 / 区间内无可绘制值，
  写错公式画布不会崩，错误与警告都从 `expressionDiagnostics()` 取。
- 三个新页面都接进了交互审计与悬停实测；示例首页补齐了 6 个大屏入口。

### 新增：柱形**逐项配色**

- 数据项写成 `{ value, color }` 时按项取色（`bar` 系列）。「红涨绿跌」「越限标红」
  「按档位分级」这类大屏常见需求，以前只能把一个系列拆成多个（还会把柱子排成一组一组），
  现在一个系列就够。级联图（瀑布）的增/减/合计取色优先级不变，逐项配色只在没被级联覆写时生效。

### 修复

- **树图的标签会横着压到隔壁块上**（大屏实测）。两个原因叠在一起：
  1. 父节点标签画在标题带里、是左对齐的，画完 **没有把 `ctx.textAlign` 还原** ——
     后面的叶子标签就变成「从矩形中心往右排」，等于整体右移半个文本宽；宽块看不出来，
     窄块（40~70px）直接把字顶出画布。
  2. 「放得下才画」以前只按矩形尺寸判断（< 44px 宽不画），而树图的占比每轮都在变，
     44~70px 之间浮动时 4 个汉字的名字照样溢出。

  现在叶子标签显式恢复居中对齐，并按**真实文本宽度**判断：先按主题字号试，放不下逐级缩到 9px，
  仍放不下才不画（与饼图「放不下不标注」的规则一致）。
- 交互审计的「越界墨迹」探针会把**居中的图例**误判成越界：图例色块本来就是饱和色，
  而等比坐标（`aspect: 'equal'`）会把绘图区缩成正方形并居中，左轴带随之变宽、正好罩住图例。
  现在左右轴带从图例下沿开始扫（图例在下方时则扫到图例上沿为止）。

## 0.16.0

### 新增

- **`liquid` 水位球**（`type: 'liquid'`）：一个圆里装水，水位随数值升降、水面是持续起伏的波浪
  （两层错相位波浪 + 径向渐变 + 外圈）。数据大屏里最有辨识度的一张图，之前库里缺。
  入场从 0 涨上来、更新时从**当前水位**涨落；波相位用 `Date.now()` + `keepAnimating` 持续重绘，
  动效偏好 `instant` 时相位固定为 0（截图可做像素比对）。整球可命中，提示框给数值与水位百分比。
  配置：`liquid: { min, max, color, borderColor, borderWidth, waveHeight, waveLength, waveSpeed, unit, detail }`。
- `sankey.linkOpacity`：连线填充不透明度（默认仍 0.42）—— 深色底上可以调高，否则连线发闷。
- 悬停实测的类型覆盖到 **18 种**（新增 liquid）。

### 改进：大屏整体重做（`examples/dashboard.html`）

先定下大屏的视觉基调，再动手：

- **严格 12 列栅格**：12 × 120px + 12px 间距 = 1572px 设计宽，面板宽度一律是列宽的整数倍 +
  同一个间距，画布宽 = 面板宽 − 内边距 16 − 边框 2。实测所有面板只有 4 个左边缘（相差恰好 3 列）、
  4 个右边缘，12 张画布的左右留白都是 9px —— 这是「看起来对齐」的硬指标。
- **配色统一成一套大屏基调**：近黑蓝底（#030711）+ 青色主色（#00E5FF）+ 蓝/绿/黄/橙/红强调；
  点阵背景 + 顶部光晕；面板是半透明深蓝渐变 + 1px 青色描边 + **四角 L 形亮角**；
  标题栏 3px 强调条；KPI 做成一条整宽面板 + 竖直分隔线 + 30px 等宽数字。
- 图表主题统一包了一层：网格线/坐标轴/提示框/准星取色全部换成大屏配色。
  注意**坐标轴文字用近灰**（`#8b98a9`）：太蓝会被「越界墨迹」审计判成饱和色（阈值是通道极差 45）。
- 拓扑面板加高到 210px 并调小节点/加大斥力（节点两两最小间距 43px、零重叠）。

### 修复（做大屏时暴露的三处）

- **非直角坐标图在数据更新时启动「永不结束的 y 域过渡」**：极坐标 / 雷达 / 仪表盘 / 水位 /
  树图这些场景不按 y 轴排布，数据一更新就启动域过渡 → 每帧一次全量重建，而且因为
  每次 `setData` 都把系列进度重置，过渡永远到不了终点。实测大屏里 5 张图常驻重建循环
  （域过渡数一直非 0）。现在只对 `kind === 'cartesian'` 启动域过渡。
- **仪表盘 / 水位图在高频更新下渲染值永远追不上数据**：它们只由 `progress` 和目标值推导几何，
  起点恒为最小值/0 —— 监控大屏每 130ms 更新一次、更新动画 420ms，实测 CPU 79.9% 时
  水位只到 27%、指针停在 10。现在更新时把「此刻渲染值」记为起点（不受 `preserveAnimation`
  影响，那个参数只管值插值），指针/水位从当前位置继续走。新增 `renderedValue()` / `levelRatio()`
  作为可断言的渲染态。
- **`finishAnimations()` 会冻住循环动效**：蚂蚁线 / 桑基流动 / 水位波浪都挂在 `__tick` 循环补间上，
  收尾会把它们永久停掉（`keepAnimating` 的 `loopRegistered` 还是 true，不会再注册）。
  现在只收尾一次性补间。

## 0.15.0

### 新增

- **`examples/dashboard.html`：运营监控大屏**（独立示例）。深色主题 + 自绘大屏外壳（KPI 卡片 /
  面板标题栏 / 告警亮边），**11 张图共用一条数据流**：
  滑动窗口折线、实时延迟（两张用 `linkCharts` 三路联动）、仪表盘、雷达、滚动热力图、桑基（flow）、
  玫瑰图、服务拓扑（可拖节点）、矩形树图、转化漏斗、实时 K 线。
  结构性图表走更新动画做**重排过渡**，CPU > 85% 时面板亮红边 + 顶部告警点亮起，
  底部挂序列化 JSON 面板（11 张图切换查看真实快照）。

### 修复（做大屏时暴露的三处）

- **深色主题下桑基/关系图的节点名糊成白团**：文字描边写死 `rgba(255,255,255,0.85)`，
  深色底上是一层白糊。新增主题字段 `labelHaloColor`（浅色 = 白，深色 = 深色），
  桑基与关系图的**文字**用它；图形自身的白色描边（节点边框、扇形分隔线）保持不变。
- **桑基流动虚线在粗连线上变成白色珠子**：线宽 `link.width * 0.28` 没有上限，
  大屏里连线宽 40~60px → 虚线宽 12~17px。现在按设备像素封顶 5px。
- **非直角坐标场景数据更新会丢掉悬停**：雷达 / 饼图用 `tooltip.trigger: 'axis'` 时，
  `refreshHover` 对非 `cartesian` 场景直接清空悬停 —— 数据一更新（大屏上的雷达每 0.5s）
  悬停反馈就被踢掉，`highlightT` 永远起不来（实测卡在 0.01）。现在非直角坐标也按
  x 值重新解析「列」，只有直角坐标才做「列像素是否还在绘图区内」的越界检查。

### 测试

- `tests/interaction/feedback-animations.test.ts` 增补「非直角坐标场景数据更新也不丢悬停」。
- 悬停实测新增 dashboard 页（168 探针），并给探针加了「重算坐标 + 重试一次」——
  数据流页面的图元会在两次采样之间移动，探针落在已缩小的扇形外是**探针**问题，不是交互缺陷。

## 0.14.0

### 新增

- **`chart.appendData(id, items, { maxPoints, animate })`**：实时数据流的追加通道。
  追加到末尾、超过 `maxPoints` 从头裁掉（滑动窗口），x 轴窗口自动右移；
  走常规更新路径 + `preserveView`，派发 `data:change`。
  **默认不做值插值**：窗口滑动会让下标整体前移，插值会把每个点拖向邻居的值（像被拖住）；
  流畅度交给推送频率（60Hz 推送 = 60fps 平滑滚动）。
  传 `undefined` 会「碰巧」命中第一个没有 id 的系列，已显式拦掉。
- `examples/live-stream.html`：四条曲线共用一个数据发生器 ——
  滑动窗口折线（CPU/内存）+ 弹簧指针仪表盘 + 每 250ms 左移一列的滚动热力图 +
  最后一根实时跳动的 K 线；带暂停 / 1×2×4× 速度 / 注入尖峰 / fps 与已推送点数统计。
- `tests/chart/stream.test.ts`（9 例）：窗口裁剪、x 窗口右移、默认不插值、显式开插值、
  `preserveView` 与 `data:change`、参数校验、连续推 200 次的冒烟与末点命中、多系列互不干扰。

### 修复

- **数据更新会把正在跑的悬停反馈动画冲掉**：`playStage` 用**整体替换**写
  `component.props.animations`，把 `highlightT`（悬停反馈）与 `__tick`（参数扫动）一起清掉 ——
  数据流场景最明显：每帧 `appendData` 都重播 update 阶段，悬停反馈被反复清空，
  `highlightT` 永远到不了 1（实测卡在 0.16，悬停探针因此判失败）。改成**合并**写法，
  与 `setHoverIndex` / `keepAnimating` / `syncTicks` 的写法一致，并补了回归用例。

### 实测

2 系列、每 tick 各追加 1 点：窗口 120 点 **1.6ms/tick**（p95 2.4ms）、600 点 4.9ms、1500 点 11ms。

## 0.13.0

补齐 mini MATLAB 的第三种经典图：**极坐标 r(θ)**（MATLAB 的 `polarplot`）。

### 新增

- **`polarExpression` 简写**（挂在 `parametric` 类型上）：写 r 的公式即可，
  组件按 `x = r·cosθ, y = r·sinθ` 展开成参数曲线 —— 玫瑰线 / 心形线 / 螺线 / 毛毛虫直接可用，
  采样、自适应细分、命中、提示框、表达式诊断全部复用既有链路。
  诊断按 **r(θ) 本身**报（不是拿派生的 x/y 判断），提示信息带 `r(θ)：` 前缀。
- **极坐标网格 `polarGrid`**：同心圆 + 角度辐条 + 半径刻度（0° 在 3 点方向）。
  圆心取**比例尺映射后的原点**（数据域不对称时原点不在中心），半径取「原点到最近边界」——
  保证同心圆完整落在绘图区内。只在 `aspect: 'equal'` 时显示（否则圆是椭圆、网格对不上）。
  配置：`{ splitNumber, spokeCount, startAngle, showLabels }`。
- `examples/mini-matlab.html` 新增「极坐标 r(θ)」卡片：玫瑰线 / 心形 / 螺线 / 毛毛虫四个预设一键切换。
- `tests/chart/polar-function.test.ts`（7 例）：几何（像素半径 = r × 单位长度）、
  等比坐标、网格几何（半径 = 到最近边界）、未开等比时不出网格、命中与提示框。

## 0.12.3

### 修复（依赖引擎 1.4.5）

- **悬停在「画布上方内容变高」之后整体错位**：页面在画布上方插入提示条 / 错误信息时，
  画布被往下推，而引擎只在非移动事件刷新输入矩形 —— 之后每次 `mousemove` 的坐标都偏移同样的距离，
  悬停 / 命中 / 拖拽全部错位，直到用户点一下或滚一格。已在 **ice-render 1.4.5** 修掉
  （移动事件改走 `ICE.refreshInputRect()`，每帧重读一次 rect，实测 0.22µs）。
  本包 peer 依赖同步提升到 `^1.4.5`。
- 示例页 `mini-matlab.html` 的提示区放在画布下方（结构上也不再依赖上面的修复），
  两种做法叠加，旧引擎上也不会错位。

## 0.12.2

回答一个问题：**怎么知道用户有没有胡乱输入公式**。

### 新增

- **表达式诊断 `chart.expressionDiagnostics()`**（`src/expr/diagnostics.ts`，纯函数）：
  三层检查合成一份结构化结果，交给表单标红 / 提示：
  1. **语法层**：编译失败（带字符位置），按 `ExpressionErrorCode` 分类
     （`syntax` / `unknown-character` / `unknown-function` / `arity` / `empty`）；
  2. **静态层**：`unknown-variable`（**以前最坑的一类**：`b*sin(x)` 里 b 没定义，
     曲线静默消失，用户只看到一片空白）、`unused-parameter`（参数定义了没用上）；
  3. **运行层**：`no-finite-values`（整段开不出来：负数开方 / 对 0 取对数 / 分母恒为 0）、
     `constant-value`（输出恒定，画出来是一条水平线）。
- 严重级别区分 `error`（画不出来）/ `warning`（画出来了但多半不是本意）；
  `errorsOf()` / `warningsOf()` 便于筛选；`expressionErrors()` 保留为「只要错误」的快捷方式。
- 示例页加了「故意写错看看」三个按钮（缺右括号 / 未定义变量 / 整段开不出来），
  现场演示三类提示；状态行给出「已绘制 2 / 3 条曲线，1 条有错误」。

### 修复

- **级联报错**：`b*sin(x)` 以前会同时报「未定义变量」和「整段画不出来」，
  用户看到的是症状不是根因。现在有静态错误时不再跑运行层检查。

## 0.12.1

补齐「数学图」最要紧的一块：**等比坐标**（MATLAB 的 `axis equal`）。

### 新增

- **`aspect: 'equal'`**：x / y 一个数据单位在屏幕上等长，绘图区同时收缩成正方形
  （只收缩绘图区不够 —— x 跨度 2、y 跨度 10 时单位长度仍然不等，圆还是椭圆；
  只拉数据域也不够 —— 网格会空一大片、曲线缩在中间）。
  类目轴不参与（把类目间距拉齐没有意义）。
- `examples/mini-matlab.html` 参数曲线卡片加「等比坐标」开关，可直接对照拉伸前后。

### 修复

- **参数曲线被拉伸成椭圆**：之前画单位圆实测 x 方向 438px/单位、y 方向 153px/单位，
  **拉伸 2.86 倍** —— 圆看起来是椭圆，心形线看起来又扁又宽。开 `aspect: 'equal'` 后两边都是 153px/单位。

## 0.12.0

新增两种「数学图」类型：`function`（y = f(x)）与 `parametric`（x(t), y(t)），
配一个自研表达式引擎 —— 也就是用户要的「迷你 MATLAB」。

### 新增

- **表达式引擎**（`src/expr/expr.ts`，**不用 `eval` / `Function`**，CSP 安全）：
  词法 → 递归下降 → 编译成闭包；支持 `+ - * / % ^`、一元正负、常用数学函数、
  常量 `pi/e/tau`、MATLAB 习惯的隐式乘法（`2x` / `3sin(x)` / `2(x+1)`）。
  报错带位置指针；编译产物与采样器都带缓存（参数扫动时每帧上万次求值不重新解析）。
- **曲线采样**（`src/expr/sample.ts`）：均匀打底 + 按曲率自适应细分（有上限，奇异点不会失控）；
  极点写成 NaN 断点，`tan(x)` / `1/x` 不会连出竖直假线；`robustRange` 用 IQR 剪掉离群尖峰。
- **`function` 系列**：`expression` + `domain` + `params`；按**可视区间**重采样，
  y 轴数据域按可视区间自动贴合（显式 `yAxis.min/max` 或缩放过 y 时以用户为准）。
- **`parametric` 系列**：`xExpression` / `yExpression` / `domain`；
  x 轴数据域取 `x(t)` 的极值而不是参数 t（否则曲线整条错位）。
- **参数扫动**：`sweep: { name, from, to, duration, mode }` 让指定参数随时间来回，
  曲线连续变形（`keepAnimating` 每帧重算；实测 3 条曲线同屏 58fps）。
- **`chart.expressionErrors()`**：表达式编译失败不让图表崩，把错误交给表单标红。
- `examples/mini-matlab.html`：表达式输入框、参数滑杆、扫动开关、范围/采样密度切换、
  参数曲线三种形状（李萨如 / 螺线 / 心形线）+ 序列化 JSON 面板。

### 修复

- **滚轮缩放在数值轴上是反的**：`factor` 被定义成「缩小时 > 1」，类目轴用除法（方向对）、
  数值轴与视口缩放用乘法（方向反）—— 同一次滚轮在不同轴上行为相反。
  现在统一成「滚轮向上（deltaY < 0）= 放大，factor > 1 = 放大」，与引擎 `zoomAt` 的约定一致。
  这个 bug 是画函数图时才暴露的（函数图是数值 x 轴，之前的手工验证都在类目轴上）。

## 0.11.0

把动画补到**坐标轴**与**图例切换**：这两处此前是全图里仅剩的硬切。

### 新增

- **坐标轴刻度过渡**：缩放 / 平移 / 数据更新 / resize 时，刻度不再硬切 ——
  保留的刻度从旧位置滑到新位置、新出现的淡入、消失的淡出。
  **网格线与刻度共用同一条时间线**（网格位置直接读坐标轴的渲染位置），
  不会出现「标签在滑、网格线在跳」。判断完全基于「刻度集合有没有变」，
  调用方不需要传意图；连续滚轮缩放时从当前渲染位置接着走（等价指数平滑），不会每次都从旧位置重跳。
  `Axis.renderedTickPos()` / `morphing()` 对外暴露渲染态，供测试与审计断言。
- **图例切换的重排过渡**：`toggleSlice`（饼图扇区 / 漏斗阶段）与 `toggleSeries` 现在走更新动画 ——
  隐藏一个扇区时其余扇形平滑挪位、被隐藏的那个收拢到 0 再消失；漏斗阶段平滑上移；
  系列显隐时数值域与其它系列一起过渡。
- **准星跟随时长可配**：`Crosshair.followDuration` 接到 `animation.update.duration`，全图过渡手感一致。
- 测试：`tests/chart/axis-morph.test.ts`（4 例）+ `tests/interaction/legend-morph.test.ts`（3 例）。

### 修复

- **`finishAnimations()` 收不干净**：此前只收尾系列，坐标轴 / 网格 / 提示框 / 准星的补间还在跑，
  截图与「瞬时模式」会拍到半路状态。
- **饼图扇形端点分别插值会抖**：两个端点各自走最短路径，被隐藏的扇形会先变宽再收拢。
  改成「起点角 + 扫过角」分别插值，扫过角单调收敛。
- **十字准星跟随时长写死 110ms**：测试采样时机不稳导致偶发失败，现在从配置读取（默认 140ms）。

## 0.10.0

把动画延伸到**交互过程**：不只是「图怎么进来」，还有「鼠标动的时候图怎么响应」。

### 新增

- **悬停反馈动画（覆盖全部 15 种图表）**：`SeriesBase.setHoverIndex()` + `hoverBoost()` /
  `drawHoverOverlay()`，由交互层在悬停变化时统一下发，移开反向回落。
  柱形沿基线变长、气泡放大、饼图/玫瑰图扇形脱出、漏斗整级摊开、雷达顶点鼓起、关系图节点鼓起；
  K 线 / 箱线图 / 热力图 / 矩形树图叠一层提亮 + 描边（这些图元紧挨着，改宽高会让命中与渲染分叉）。
  `BarSeries.barDrawRectAt(i, t?)` 暴露「含悬停放大的绘制矩形」，便于测试断言渲染态。
- **提示框淡入淡出**：出现淡入 + 上滑 6px，消失淡出到 0 再清内容；
  淡出途中重新悬停会从当前透明度续上（此前是硬切）。
- **准星平滑跟随**：换列时从当前位置补间，`pixelX/pixelY` 保留目标值供审计/测试读取。
- **框选蚂蚁线**：`ChartComponent.keepAnimating()/stopAnimating()`（复刻引擎 lineDashFlow 的做法），
  有选框才持续重绘，松手即停。
- **桑基流动**：`sankey: { flow, flowSpeed }`，沿连线路径描一层流动虚线表达方向。
- **矩形树图形变**：数据更新时在旧布局与新布局之间插值；子节点按**相对父矩形的比例**套在父的插值结果上，
  中途不会出现「兄弟节点拼不满父矩形」的缺口。
- `tests/interaction/feedback-animations.test.ts`：6 个用例覆盖上述全部反馈路径。

### 修复

- **提示框首次出现不淡入**：`panelOpacity()` 在还没有动画状态时返回 1（向后兼容默认值），
  导致第一次的补间是 `1 → 1`。现在动画起点用独立的 `currentOpacity()`（无状态视为 0）。
- **淡出途中被重新唤起会卡在半透明**：`show()` 只在「此前没有内容」时才启动淡入，
  现在只要仍在淡出中或透明度 < 1 就重新淡入。
- **箱线图把原始观测值当成五数概括**：判断条件写的是 `tuple.length >= 5`，
  于是「40 个观测值」被截成前 5 个当作 `[min, Q1, median, Q3, max]`，
  箱体的 min/max 包不住四分位（图形错乱），命中判定也跟着失效。改成**恰好 5 个数**才按五数概括解释。
  这个 bug 只有真的用浏览器去悬停才看得出来（单测当时只喂了 5 个数的样本）。
- **像素缓存不失效导致悬停高亮画在别处**（系统性）：多个系列的缓存键只拼了「绘图区 + 某一个轴的数据域」，
  缩放 / 数据域过渡 / 轴类目变化之后 `pixels` 还是旧位置 —— 渲染用的几何（重算）与
  命中/高亮锚点用的几何（`pixels`）就此分叉。现在 `SeriesBase.buildSeriesKey()` 统一把
  **坐标系指纹**（绘图区矩形 + x/y 比例尺的数据域端点与长度）拼进键里，子类不用再各自记得。
  新增 `tests/components/pixel-freshness.test.ts` 作为门禁。
- **数据更新时悬停被清掉**（实时刷新场景）：`refreshHover()` 拿着**上一轮**的系列对象去 `buildActiveItem`，
  而 `seriesComponentOf` 按对象身份匹配，重建后的 norm 里已经换了新对象 → 匹配失败 → 悬停被当作失效清理。
  仪表盘这种 1.6s 刷一次数据的图表，鼠标停在上面会不停丢悬停。现在按 id 兜底匹配，并从当前 norm 取新系列。
- **柱形悬停放大与高亮描边错位**：柱子悬停时会长一点，而高亮描边用的是没长大的基础矩形，
  看起来像「柱子里横着一条线」。现在描边取终态绘制矩形（`barDrawRectAt(i, 1)`）并与绘图区求交。

### 已知取舍

- 柱形 / 散点气泡 / 饼图 / 玫瑰图是**几何变形**（变长 / 变大 / 脱出）；
  漏斗（整级摊开）、雷达（顶点鼓起）、关系图（节点鼓起）同理；
  K 线 / 箱线图 / 热力图 / 矩形树图是**叠加提亮 + 描边**（这些图元彼此紧挨，
  改宽高会让命中区域与渲染区域分叉，指针停在边缘会来回抖）。

## 0.9.0

把引擎的动画能力真正接进图表（此前只有一个 `progress` 字段）。

### 新增

- **三段动画配置**：`animation.enter / update / highlight`，每段可配 `duration / delay / easing / stagger`，
  也可单独用 `false` 关掉；保留扁平写法的向后兼容。
- **错峰入场（stagger）**：每个数据项用自己的进度（`SeriesBase.itemProgress`），
  波浪式依次进场，所有项仍在同一时刻结束。
- **动效偏好（无障碍）**：`setMotionPreference('auto' | 'instant' | 'full')`，
  `auto` 跟随 `prefers-reduced-motion`；`chart.finishAnimations()` 推到终态。
- **更新动画**：`setData` / `setOption` 时系列值从旧值插值到新值（此前是瞬跳）。
- **坐标轴数据域过渡**：更新动画期间 y 轴数据域与系列值同步插值，避免「域瞬跳导致图形先蹦一下」。
- **逐类型入场形态**：柱形错峰长出、折线从左到右画出、气泡弹簧弹出、扇形依次扫开、
  K 线从开盘价展开、箱线图从中位线展开、热力图对角线浮现、漏斗从等宽收拢、
  仪表盘指针扫值、关系图从环形收敛到力布局结果。
- `examples/animation.html`：五张图 + 重播 / 换数据 / 错峰开关 / 动效偏好切换。

### 修复

- **首次渲染不播动画**：构造函数里传了 `animate: false`，`playEnter` 只在后续更新时才触发。
- **动画期间几何被冻住**（系统性）：六个系列各自覆写 `rebuildPixels` 时，缓存键里没有动画进度，
  缓存命中后几何停在动画第一帧。统一改用 `SeriesBase.buildSeriesKey()`（把 progress / 阶段 / 错峰写进键）。
- **`finishAnimations()` 无效**：只把 progress 置 1 而不取消引擎补间，下一帧又被写回去。
- **域过渡期间清掉了动画起点**：轻量同步仍调用 `updateSeries(series, false)`，
  现在支持 `preserveAnimation` 参数，域过渡只换引用、不动插值起点。

### 实测结论（纠正一条我此前的担心）

- 引擎在补间期间把 `interactive` 置 false，**只发生在同一帧的同步块内**（保存→置 false→补间→恢复），
  事件处理与命中检测看不到它 —— 逐帧采样实测：动画进行中 `hitTest` 始终命中系列。
  因此**不需要**用「不可见驱动组件」绕开它，动画与交互可以同时成立。

## 0.8.0

新增力导向关系图（无底图）。

### 新增

- **`type: 'graph'`**：节点 + 连线 + 力学布局，不依赖任何底图。
  - 布局是纯函数 `forceLayout`：斥力（∝1/d²）+ 弹力（沿连线）+ 向心力，带冷却与阻尼；
    迭代结束后按包围盒**等比铺满绘图区**（否则力布局常挤在中间一小块）。
  - 初始位置用**环形均匀铺开而不是随机** —— 结果可复现、可写断言，同一份数据每次渲染一致。
  - 节点按权重定大小（面积映射，避免大权重夸张）、按分类/颜色配色；连线按流量定粗细，可选曲线。
  - **节点可拖拽重排**：按住节点跟手移动，松手后跑少量迭代让邻居跟随（被拖的节点保持固定）。
  - 命中返回节点下标或 `节点数 + 连线下标`（与桑基图同一约定）；提示框给权重/连接数、连线给两端与流量。
  - `layout: 'circular' | 'none'` 也支持：环形布局，或直接用给定坐标（不动力学）。

### 示例与门禁

- 新增 `graph.html`（微服务依赖图，15 节点 / 20 连线，可拖拽重排）；
  交互审计扩展到 18 页 / 198 步（当前 0 问题），23 个测试套件 / 217 个用例全绿。

## 0.7.0

补齐「构成类」图表：矩形树图。

### 新增

- **矩形树图**（`type: 'treemap'`）：层级数据（`{ name, value, children }`）用嵌套矩形表达构成。
  布局是纯函数 `layoutTreemap`，采用 **squarified** 算法（每行尽量接近正方形，避免细长切片）；
  父节点自动留出标题带，标签画在左上角而不是压在子矩形上；子节点颜色在父色基础上按层级淡化。
  命中判定返回**最深的那个节点**（点在大类目里的小类目上命中的是小类目），提示框给名称 / 数值 / 占根总量比例。

### 示例与门禁

- 新增 `treemap.html` 示例；交互审计扩展到 17 页（当前 0 问题），22 个测试套件 / 206 个用例全绿。

## 0.6.0

继续补齐常见图表类型：横向柱状图、气泡图、漏斗图、仪表盘、箱线图、瀑布图。

### 新增

- **横向柱状图**：`yAxis` 声明为类目轴（`type: 'category'` 或 `data: [...]`）、`xAxis` 为数值轴即自动转置，
  与主流写法一致。支持轴声明类目（按下标回填数据点）与对象数据的 `name`；
  横向图默认 `tooltip.trigger: 'item'` 并收起十字准星，提示框标题用类目、数值走 x 轴格式化。
  同时兼容 `type: 'value'` 这个数值轴别名。
- **气泡图**：`scatter` 的第三维 `[x, y, size]` 经 `symbolSizeRange`（默认 `[8, 40]`）映射成直径；
  `symbolSize` 也支持函数形式。尺寸解析统一收敛到 `SeriesBase.symbolSizeAt()`。
- **漏斗图**：阶段按数值排序、相邻阶段画梯形（`trapezoid: false` 退化为矩形）、
  `minSize` 保证最小阶段仍可见可点；图例项是「阶段」，复用扇区显隐机制。
- **仪表盘**：角度沿用主流习惯（0° = 3 点、逆时针为正、90° = 12 点），默认 225° → -45° 扫 270°；
  轴线按阈值分段配色、刻度与刻度值、指针（可配宽度/长度）、数值文本与名称。
- **箱线图**：`[min, Q1, median, Q3, max]`，或给原始观测值自动算五数概括（`computeBoxplotSummary`）；
  须的末端进 y 轴数据域，命中判定覆盖整条须。
- **瀑布图**：`base/top` 由累计值推导，合计项（`total: true`）从 0 画到累计值且不改变累计；
  增 / 减 / 合计三色可配，柱子之间画连接虚线。

### 变更

- `hiddenSlices` / `setHiddenSlices` 上提到 `SeriesBase`（饼图与漏斗图共用）。
- 柱形的像素锚点统一为「柱心」，并新增 `barColorAt()` / `highlightRectAt()` 两个扩展点。
- 新增 `data:change` 事件（`setData` 后触发），示例页的 JSON 面板会跟随刷新。

### 修复

- 横向柱状图的槽位偏移没有跟随 y 轴 range 方向，导致最下方那根柱子越过绘图区底边（像素探针定位）。
- 柱形按数据下标定位类目、`BandScale` 的数值兜底把数值类目当下标（缩放后柱子锚错位置）。

### 示例与门禁

- 新增 `horizontal-bubble.html`、`funnel-gauge.html`、`boxplot-waterfall.html` 三个示例页；
  交互审计扩展到 16 页 / 176 步，当前 0 问题；21 个测试套件 / 197 个用例全绿。

## 0.5.0

每个示例页新增「序列化 JSON」面板，实时显示图表的真实快照。

### 新增

- `examples/assets/snapshot-panel.js`（`mountSnapshotPanel()`）：把 `chart.toJSON()` 的真实内容
  渲染成带语法高亮的 JSON 面板，含版本 / 体积 / 系列数 / 更新时间与 刷新 / 复制 / 下载。
  订阅 `zoom:change` / `pan:change` / `legend:toggle` 自动刷新（150ms 防抖）；
  多图页面（柱形堆叠、饼图、雷达、K 线+热力图、联动、序列化）提供图表切换 tab。
- 大负载保护：5 万点的完整快照 3.7 MB / 11 MB（美化），面板改为
  「截断长数组的预览 + 完整内容供复制下载」，并标明两者体积。
- 交互审计新增面板检查：每个示例页必须存在可解析且带 `version` / `option` 的快照。

## 0.4.0

把「图表可序列化 / 可反序列化」做成显式契约，并给出可复现的证据。

### 新增

- `chart.toJSON()` 增加 `version`；新增 `ChartSnapshot` / `SnapshotRestoreOptions` 类型。
- `ICEChart.restore(target, snapshot, options)`；`createChart(target, snapshotOrJson)`
  现在能直接吃快照（自动识别并走还原路径）。
- `fromJSONObject(snapshot, { optionPatch })`：还原时补回无法进 JSON 的函数字段（formatter 等），
  series 按 id 或下标逐项合并，不覆盖快照里的数据。
- `chart.toDataURL()` / `chart.toBlob()`：透传引擎的图片导出能力。
- 导出 `isChartSnapshot` / `mergeOptionPatch` / `SNAPSHOT_VERSION`。
- `examples/serialize.html`：导出 → 还原 → **逐像素比对**，并显示快照体积与摘要。

### 证据

- 7 种场景（直角坐标 / 多 y 轴 / 饼图 / 雷达 / K 线 / 热力图 / 桑基）导出再还原后语义一致，
  且再导出的 JSON 与原文逐字节相同（幂等）。
- 浏览器实测：缩放窗口 `[8,20]` + 隐藏系列后还原，两张画布 `toDataURL()` **完全一致**。
- 明确记录设计边界：`ice.toJSONString()` 的组件树**不是**图表持久化格式
  （实测还原后 `unknownTypes` 列出全部 chart 组件、只落地一个根节点）。

### 修复

- `mergeOptionPatch` 里「带 id 的补丁在下标不匹配时退化按下标合并」会把快照的 A 系列
  改名成 B（已补回归用例）。

## 0.3.0

### 修复：图形越出坐标轴（用户实测反馈）

- **系列绘制未裁剪到绘图区**：数值轴/时间轴缩放后，窗口外的数据点会被线性外推到画布之外，
  墨迹一路盖住 y 轴刻度、图例与坐标轴。现在所有直角坐标系列在 `beginDraw()` 里裁剪到自身盒
  （= 绘图区）。实测大数据页：缩放后 y 轴标签区的彩色墨点由 **2554 → 0**。
- **柱形按「数据下标」定位类目**：类目轴缩放后可见窗口是类目的子集，用下标会被当成窗口内位置，
  把窗口外的柱子（及其高亮框）画到坐标轴上。改为按类目值定位。
- **`BandScale` 的「数值兜底」**：把数值类目当成下标，数值类目轴缩放后柱子会锚到错误类目上。
  该兜底本就被字符串比较覆盖，已删除。

### 审计脚本自身的盲区

- 缩放审计步骤用的是 `wheel(0, 200)`（缩小），在满窗口时是空操作，因此从没真正测到缩放后的画面。
  现改为放大 4 档，并新增**像素级越界检查**：统计左右轴标签带与坐标轴标签带里的「饱和色墨迹」，
  任何一条超过阈值即判失败（图例色块与 dataZoom 滑块分别在绘图区上方/下方，已被排除）。

视觉改为 Bootstrap 风格，并把「交互过程中的外观」变成可自动断言的门禁。

### 变更

- **默认主题换成 Bootstrap 5**：系列色序取 primary / success / danger / info / indigo /
  warning / orange / teal / pink / secondary，坐标轴与网格用 gray-100~900，
  字体用 Bootstrap 的 `--bs-body-font-family`，提示框圆角用 `--bs-border-radius`。
  新增 `BOOTSTRAP_CHART_THEME` / `BOOTSTRAP_DARK_CHART_THEME` / `BOOTSTRAP_TOKENS` 导出，
  旧的 `LIGHT_CHART_THEME` / `DARK_CHART_THEME` 保留为别名。
- 示例页样式对齐 Bootstrap 变量（字体、边框色、卡片圆角与阴影、链接色）。

### 修复（全部由「交互外观审计」发现）

- 缩放 / 平移 / 滑块拖动后，十字准星与高亮环停在旧像素位置、和曲线脱开 → 数据域变化后重新定位悬停。
- 键盘导航会走到**可见窗口之外**的数据点，准星被画到绘图区外、压住 y 轴刻度 → 只在窗口内移动，越界即清理。
- 提示框会压住十字准星的轴数值标签，贴底时还会压住坐标轴 → 纵向让开标签带、横向优先留在绘图区内。
- 隐藏正在悬停的系列后，提示框跳到别的系列（或留下残影）→ 立即清理。
- 框选 / 拖滑块期间提示框与准星跟着抖动 → 手势期间收起，松手后按指针位置恢复。
- 悬停图例项没有任何视觉反馈（高亮被自己的清理逻辑抹掉）。
- 高亮标记的锚点语义不一致（圆环是中心、柱形是左上角）→ 统一为中心。
- 玫瑰图小扇区的内部标签互相叠压 → 按相邻角度逐级外推，放不下就不画（交给图例与提示框）。
- 桑基节点名压在连线上不可读 → 加白色描边光晕。

### 新增

- `scripts/audit-interactions.mjs`（`npm run audit:interactions`）：对 12 个示例页跑 10 步真实交互
  （悬停 / 贴底悬停 / 边缘悬停 / 滚轮缩放 / 拖拽平移 / 框选 / 键盘 / 图例切换 / 切换后再悬停），
  每步截图并断言「提示框不越界、不压坐标轴标签、不压图例、高亮在绘图区内」。
- `tests/interaction/appearance.test.ts`：把同样的断言固化成 7 个 jsdom 用例。

## 0.2.0

把工具箱补齐到覆盖绝大多数业务图表，并补上大数据与无障碍。

### 新增

- **多 y 轴**：`yAxis` 支持数组，系列用 `yAxisIndex` 绑定；每个轴独立数据域与刻度，同侧多层外移；`setAxisDomain()` 按轴控制窗口。
- **极坐标与饼图**：`type: 'pie'`，支持环形（`innerRadius`）与玫瑰图（`roseType`）；扇形命中、引导线标签、按扇区的图例联动与百分比重算。
- **雷达图**：`radar.indicators` + `type: 'radar'`，支持多边形 / 圆形网格；顶点命中，提示框一次列出全部指标。
- **dataZoom 滑块**：轨道 / 窗口 / 双手柄，支持拖窗口、拖手柄、点轨道；比例窗口 ↔ 数据域的换算留在图表层。
- **大数据降采样**：折线 / 面积按 LTTB 抽稀（保留首尾与尖峰），命中判定用二分查找。
- **无障碍**：`getDataTable()` / `getA11yTree()` / `attachA11yMirror()`，视觉隐藏数据表 + `aria-describedby` + `aria-live` 播报。
- **K 线**：`type: 'candlestick'`，`[open, close, low, high]`，影线进入数据域，提示框给 OHLC。
- **热力图**：`type: 'heatmap'`，`[x类目, y类目, 数值]`，y 轴自动变类目轴，颜色线性插值。
- **桑基图**：`type: 'sankey'`，纯函数分层 + 纵向松弛布局，节点/连线分别命中。

### 修复

- 类目轴缩放会把坐标轴塌缩成两个类目（K 线蜡烛异常变宽即由此暴露）。
- 左右两侧图例的尺寸计算把 `0` 当作初始最小值，导致绘图区被挤成 20px。
- 同一页面上多张图的事件串扰：非本画布的指针事件会清掉镜像过来的悬停。
- `ICEEvent.preventDefault()` 在部分路径下抛 `Illegal invocation`。
- 自定义组件的 `doRender()` 未 `beginPath()`，把引擎脏矩形裁剪残留路径一起描了出来。
- `BandScale` 在倒置 range（y 轴）下带宽为负，热力图整块落到绘图区外。
- 雷达网格组件误减 `plot` 偏移导致整体错位。

## 0.1.0

首个版本：把 ice-render 的事件系统与交互能力带进图表场景。

### 新增

- `createChart` / `ICEChart`：把声明式 option 编译成 ice-render 组件树。
- 比例尺：linear / category（band）/ time / log，含 nice 刻度与自适应时间粒度。
- 系列：line / area / bar（分组 + 堆叠）/ scatter，支持平滑曲线、断点、虚线、渐变填充。
- 组件：PlotArea / GridLines / Axis / Title / Legend / Tooltip / Crosshair / Highlight / Brush。
- 交互：悬停（axis / item 触发器）、十字准星、压暗高亮、单选 / 多选、框选（select / zoom）、
  滚轮缩放（以指针为锚点）、拖拽平移（约束在完整数据域内）、键盘导航。
- 跨图联动 `linkCharts`：悬停 / 缩放 / 平移 / 框选按数据值对齐，并用 `silent()` 打断回环。
- 图例点击切换系列显隐并重算数据域；`toJSON` / `fromJSONString` 序列化。
- 主题：light / dark / 自定义片段，色板取自 ice-render 设计 token。
- 工具链：Rollup（ESM + CJS + UMD + .d.ts/.d.mts）、jest（jsdom + Canvas 2D 桩）、eslint、prettier。
- 示例：基础折线 / 面积、分组与堆叠柱形、交互总览、时间轴 + dataZoom、跨图联动。
